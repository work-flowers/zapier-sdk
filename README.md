# zapier-sdk

Source-of-truth repo for **workFlowers** Code Zaps in the main work.flowers Zapier workspace — Durables (`@zapier/zapier-durable`) plus one classic Code-step Zap.

## Repo structure

One sub-directory per Zap. Each Durable directory contains:

| File | Purpose |
| --- | --- |
| `workflow.ts` | The durable workflow source, as published on Zapier |
| `zap.json` | Deployment metadata: workflow ID, current version ID, trigger URL, runtime/dependency versions |
| `README.md` | Brief description of the Zap: what it does, trigger, a Mermaid diagram of the workflow, maintainer notes |

Classic Code-step Zap directories carry the code-step source and tests instead of `workflow.ts`/`zap.json`; everything else applies unchanged.

A Zap with an AI step additionally carries a `*-prompt.md` holding the prompt (repo rule 6). Repo-wide helper scripts live in [`scripts/`](scripts/):

| Script | Purpose |
| --- | --- |
| [`build-map.mjs`](scripts/build-map.mjs) | Regenerates the **[interactive workflow map](docs/map.html)** — a pan/zoomable atlas of every Zap and the shared assets (Zapier Tables, Notion data sources, Drive folders, external apps) that interconnect them. Facts (dirs, both `zap.json` shapes, every table ULID and data-source UUID in the sources) are extracted from the repo; read/write/own/trigger semantics are curated in [`docs/map-overlay.json`](docs/map-overlay.json). Layout is computed at build time (deterministic); the generator rewrites only the marker-delimited data block in `docs/map.html`. `--check` fails when the repo and the map drift (also validates the overlay both directions) and runs as a PR check via [`check-map.yml`](.github/workflows/check-map.yml); `--validate`, `--report`, `--debug-svg <out>` for development. |
| [`check-prompts.mjs`](scripts/check-prompts.mjs) | Verifies every `*-prompt.md` matches the copy embedded in its Zap's source (`--fix` re-injects). A Zap with **more than one** prompt declares which literal each markdown owns via an `<!-- embed: NAME_PROMPT -->` marker — see [`drive-invoice-to-xero`](drive-invoice-to-xero/), which has two. |
| [`backfill-meeting-note-event-ids.mjs`](scripts/backfill-meeting-note-event-ids.mjs) | One-off: re-keys `[Table] Meeting Note IDs` from the event **series** (`iCalUID`) back to the per-**occurrence** Google event id, reading each occurrence id off the linked Notion page's `Google Calendar Event ID` so no calendar guessing is involved. Preserves the old value in the new `iCal UID` (f9) column rather than discarding it, flags rows whose Notion page is gone as `Archived`, and refuses to commit if two rows would claim the same key while pointing at *different* pages. Free to run (Notion reads are direct API calls; Table writes consume no tasks). Plan-only unless `--commit`. Ran 2026-07-31: 84 re-keyed, 3 redundant twins, 4 archived, 1 unresolved. |
| [`backfill-zapier-partner-leads.mjs`](scripts/backfill-zapier-partner-leads.mjs) | One-off: replays the partner tool's existing referral leads through [`zapier-partner-lead-status-to-notion`](zapier-partner-lead-status-to-notion/) so historical leads get mirrored into the CRM instead of waiting for their next status change. Mirrors that workflow's adoption policy, so it fires only leads that can actually land (10 of 247 today, not 241). Plan-only unless `--commit`. |
| [`audit-contact-icon-urls.mjs`](scripts/audit-contact-icon-urls.mjs) | Read-only: classifies every Notion Contact's page icon and cover as an already-expired signed URL, a signed URL still live, a plain external link, or a permanent Notion-hosted file. Written to size the damage from [`enrich-contact-records`](enrich-contact-records/) storing LinkedIn CDN links as `{type:"external"}` icons, which render as blank white space once the signature expires. First run 2026-08-12: **210 of 962 contacts broken**, 40 more on a `2147483647` ("never") expiry, and 445 on stable `assets.clay.earth` links that are fine. `--json` emits the per-page classification so a backfill can consume the affected page ids. Writes nothing; free to run. |

Repo-wide docs live in [`docs/`](docs/): [`map.html`](docs/map.html) is the self-contained interactive map of the whole workflow graph (open it locally in any browser — no server, no network), [`map-overlay.json`](docs/map-overlay.json) its curated semantics, and [`interactive-map-plan.md`](docs/interactive-map-plan.md) the build plan behind it.

`docs/` is also served by **GitHub Pages** (`main` → `/docs`), so the map is live at **https://work-flowers.github.io/zapier-sdk/map.html** — that URL is what [work.flowers/zap-map](https://www.work.flowers/zap-map) embeds in an `<iframe>`. Publishing that way is deliberate: `map.html` is a full-viewport document (`html,body{height:100%}`, `#stage{position:absolute;inset:0}`, a `position:fixed` header), so pasting its markup straight into a host page collapses the stage to a sliver and leaks `body{overflow:hidden}` into the host. It needs its own document. A merge to `main` republishes it; there is no separate deploy step.

A Zap deployed more than once from one directory keeps a single `zap.json` with a `deployments` array (see [`luma-event-to-notion`](luma-event-to-notion/)). Where those deployments need to differ in code, the shared logic lives in a module and each deployment publishes its own thin entry file as `workflow.ts` — see [`internal-user-ids-to-table-and-notion`](internal-user-ids-to-table-and-notion/). The same layout also holds one directory of *distinct but interlocking* Zaps together, where each has its own trigger and its own logic but they share helpers and Tables — see [`whatsapp-slack-bridge`](whatsapp-slack-bridge/), whose three deployments would be unreadable split across three directories.

## Repo rules

1. **Every Zap sub-directory includes a brief README** in addition to the code, always with a Mermaid diagram depicting the workflow, if possible.
2. **Deployed code always links back to this repo** — each source file carries a `// Source of truth: https://github.com/work-flowers/zapier-sdk/tree/main/<zap-name>` comment, and that comment must be present in the version published to Zapier.
3. **This root README is updated whenever Zaps are created, published, modified, enabled/disabled, or removed** — keep the Zap index below current.
4. **Every Durable directory carries a `zap.json` mirroring the deployed state** — pulled from Zapier when the Zap is added to the repo, and refreshed (`current_version_id`, `enabled`, dependency versions, etc.) whenever the Zap changes on Zapier.
5. **Creating a page in a Notion data source always applies that data source's default template, if one exists** — automation-created pages must look like hand-made ones. Pass `template_mode: "default"` on every `create_database_item` call and fall back to a plain create only when Notion reports the data source has no default template. Because a template and inline page content are mutually exclusive in one call, body content is added in a second `write/page_content` call. See the "Notion page creation" convention in [CLAUDE.md](CLAUDE.md) for the helper and the exact error to catch.

6. **Every Zap with an AI step keeps its prompt in a `*-prompt.md` file in the Zap's directory** — prompts are the part of a Zap most worth reviewing and the hardest to read inside a code literal, so the markdown is the source of truth and the deployed source embeds a verbatim copy. Run `node scripts/check-prompts.mjs` to verify the two agree (`--fix` re-injects from the markdown). A Zap with **more than one** AI step carries one markdown per prompt, each declaring which literal it owns with an `<!-- embed: NAME_PROMPT -->` marker above its `## Prompt` heading; without it the checker cannot tell two literals apart and `--fix` would overwrite one from every markdown in turn. A single-prompt Zap needs no marker. See [`gmail-attachments-to-drive-by-type`](gmail-attachments-to-drive-by-type/) for the reference layout and [`drive-invoice-to-xero`](drive-invoice-to-xero/) for the two-prompt shape.
7. **AI steps run on AI by Zapier and default to the `standard/auto` tier.** The tier *is* the task cost — standard 1×, advanced 3×, premium 5× per run — and Zapier's own Advanced default exists mainly to enable tool calls, which these Zaps don't use. A higher tier is only adopted when a test at Standard actually fails; the doubt gets raised first and the comparison recorded. Each such Zap keeps a verified-cases table in its README and an `ai_model` block in its `zap.json`. See the "AI by Zapier" convention in [CLAUDE.md](CLAUDE.md).
8. **New Zaps are created account-visible, never private** — `create-workflow` is run **without** `--private` (the SDK CLI already defaults to account-visible, so this is a matter of not passing the flag). These are shared production automations for the work.flowers workspace, and a private workflow is invisible to everyone but its creator, Zapier UI included — which makes it unreviewable and unfixable by anyone else. This deliberately overrides the `workflows-create` skill's instruction to default to `--private` for Early Access users. **Visibility cannot be changed after creation** (`update-workflow` only takes `--name`/`--description`, and no publish command exposes a visibility flag), so `create-workflow` is the only chance to get it right. The state is recorded as `is_private` in each `zap.json`. [`email-db-updates`](email-db-updates/) and [`merge-duplicate-contacts`](merge-duplicate-contacts/) are private because they predate this rule and are left as they are.

These rules are mirrored in [CLAUDE.md](CLAUDE.md) so Claude Code sessions follow them automatically.

## Zaps

| Zap | Type | Status | Description |
| --- | --- | --- | --- |
| [`merge-duplicate-contacts`](merge-duplicate-contacts/) | Durable | ✅ Enabled | Merges a Contact into the record its `Duplicate of` points at — but only when identity is corroborated (equivalent names or the same LinkedIn profile; **differing LinkedIn URLs are a hard decline**). Fills empty target fields, ORs checkboxes, **unions** every multi-value property, never copies the `Duplicate of` / `Possible duplicate` relations, and **never deletes anything**. Replaces the "Contact Merger" Notion Custom Agent, disabled 2026-07-28. Triggered by a Notion DB automation on Contacts (`Duplicate of` edited); verified live the same day on both the merge and decline paths. |
| [`contact-emails-to-zapier-table`](contact-emails-to-zapier-table/) | Durable | ✅ Enabled | Sole owner of the email → page-ID Zapier Table. Notion Contacts email edits → index every Primary/Secondary email; flags cross-contact collisions on `Possible duplicate of` (a review flag — **never** `Duplicate of`, which the Contact Merger Notion agent acts on; see that Zap's README); on trash, hands a merged-away contact's addresses to the survivor or deletes them if it was a genuine delete; on restore, re-indexes them; ignores pages from the other Core CRM Objects data sources. Port of the classic "Update Zapier Table When Email Address Updated" Zap, plus the retired Contacts branch of "Delete Contact, Company or Meeting Note Page from Zapier Table". |
| [`contrast-registrations-to-event-attendance`](contrast-registrations-to-event-attendance/) | Durable | ⏸️ Disabled | Contrast webinar registrations → Notion Event Attendance upserts. Retired 2026-07-23 when events moved to Luma; kept for reference. |
| [`deal-to-client-slack-channel`](deal-to-client-slack-channel/) | Durable | ✅ Enabled | Deal webhook from Notion → AI-shortened `deal-*` Slack channel for the company (AI by Zapier `standard/auto`, rules re-enforced deterministically), channel id written back to the Company (`Slack Channel` is now a formula rendering the URL from it), deal owner invited via [Table] Internal User IDs unless it's Dennis. Guards on the Notion page's `Slack Channel ID` rather than [Table] Company IDs — the Table is a mirror of that same property. Migration of the classic "Create Client Slack Channel from Notion"; the Notion sender was repointed and the classic Zap disabled 2026-08-10. |
| [`deal-to-client-drive-folder`](deal-to-client-drive-folder/) | Durable | ✅ Enabled | Deal webhook from Notion → create the company's Google Drive folder under Client Docs (Work.Flowers HQ shared drive), folder id written back to the Company (`Google Drive Folder` is a formula rendering the URL). Guards on the page's `Google Drive Folder ID`, not the Table. Migration of the classic "Create Google Drive Folder from Notion"; supersedes the never-deployed [`deal-won-set-up-client-workspace`](deal-won-set-up-client-workspace/). Notion sender repointed and classic Zap disabled 2026-08-10. |
| [`deal-won-set-up-client-workspace`](deal-won-set-up-client-workspace/) | Durable | ⚠️ Not deployed — superseded | Deal won → create the company's Google Drive folder under Client Docs and link it on the Notion Companies record. Source only; never published. Superseded by [`deal-to-client-drive-folder`](deal-to-client-drive-folder/) (2026-08-10). |
| [`drive-invoice-to-xero`](drive-invoice-to-xero/) | Durable | ✅ Enabled | Invoice PDF added to the Drive **Invoices** folder → AI call 1 extracts the header *and* the line items, AI call 2 extracts the vendor's **payment instructions** → rename to `<invoice date> <vendor>` → look for a matching `SPEND` bank transaction. Already paid → attach the PDF to that Xero transaction; still outstanding → raise a **draft** bill. Migration of the classic "Record New Bills in Xero"; downstream of [`gmail-attachments-to-drive-by-type`](gmail-attachments-to-drive-by-type/). Replaces the classic Zap's AI Agent with deterministic vendor-suffix normalisation, and makes **amount + currency a required match** rather than optional — Anthropic had 3 transactions within 4 days of one invoice, and a split payment (4.92 + 40.98 for a 45.90 invoice) now correctly declines instead of attaching to half of it. Adds a duplicate-bill guard and reconciles extracted line items against the invoice total, falling back to a single line so a draft bill always adds up. **Reads the `Xero Bank Transactions` Table, so it is only as correct as [`xero-bank-transactions-to-zapier-table`](xero-bank-transactions-to-zapier-table/) is current** — see the directory README. Its first published version failed on **every** PDF invoice with a `DeterminismViolation` — the durable runtime's `Date` guard rejects even a deterministic `new Date(Date.UTC(…))` in the workflow body — and its only run was Lantern Labs INV-26-0006 on 2026-07-28. Fixed in `019faae7` (integer date arithmetic; clock read inside a `today` step) and that run was replayed, raising the draft bill it should have raised first time. **Vendor-contact resolution went live in `019fab74` (2026-07-29) and has now been exercised by a real invoice** — Lantern Labs `INV-26-0007`, 46s end to end, extraction 10/10, vendor resolved to the single existing contact with no duplicate minted, and three empty fields filled correctly. `new_bill` binds a bill to a contact *by name* and silently creates a bare one, which is how the ledger acquired four duplicate pairs (`Aspire FT` / `Aspire FT Pte. Ltd.` and three more). It now resolves the vendor against the real contact list with the same suffix-normalising matcher used for bank transactions, binds the bill to the **resolved** contact's stored name, creates a missing contact with the address, phone, tax number and bank details read off the invoice, and fills only *empty* fields on one that already exists — with bank details write-once and a mismatch reported rather than applied, since an emailed invoice asking to be paid somewhere new is what invoice-redirection fraud looks like. Ambiguous names are left alone for a human to merge. Validated offline against the real 130-contact ledger (47 assertions), and that first live run also closed the bank-field control the design shipped as untested: on an invoice offering only a PayNow QR and a Wise link, the model returned **no** bank account where a hallucinated one would have been written. That run also exposed a bad tripwire: `tableStale` fired on a demonstrably healthy feeder, because it asked whether the invoice's own match window was empty and that window is mostly *future*-dated for a freshly received invoice — ~39 of its 46 days. **Fixed in `019fad1c`**, which measures the Table's actual freshness instead (a free read of the newest row, stale past 14 days behind today) and reports `tableLagDays` / `tableLatestRowDate` alongside it; see the directory README. **A second AI call now extracts the vendor's payment instructions into the new `Vendor Payment Details` Table** (`01KYR653H04DNMKKYAZ72534YG`, one row per Xero contact), which [`xero-bill-approved-to-wise-transfer`](xero-bill-approved-to-wise-transfer/) reads to build a Wise recipient — Xero cannot hold the tuple, since `BankAccountDetails` is a single free-text field while Wise needs the account number, IBAN, SWIFT, bank code, branch code and PayNow alias as separate keys. It is a *second call* rather than more fields on the first because folding them in took that call from 21 output fields to 34 and measurably broke the header: on the Slack invoice, correct vendor name scored **6/6 at 21 fields and 2/6 at 34**, the failures naming *Company Flow Pte. Ltd.* — our own entity — as the vendor. `Vendor Name` binds both the contact and the bill, so that is the most expensive field on the page to get wrong; the header call is therefore left byte-for-byte as deployed. Costs +1 task per invoice. Bank details in the new Table are write-once with the same discipline as the Xero field: a changed account number is **never applied**, it raises `needs_review`, and once a Wise recipient is cached that escalates to a hard stop the payment Zap obeys. Validated on 9 real PDFs plus 39 offline assertions run against `tsc`'s own emitted output — see the directory README. |
| [`drive-paid-receipts-to-table`](drive-paid-receipts-to-table/) | Durable | ✅ Enabled | Receipt PDF added to the Drive **Paid Receipts** folder → AI by Zapier extracts vendor/date/currency/amount → rename to `<invoice date> <vendor>` → find-or-create its row in the `[Table] Receipts` Zapier Table, keyed on File ID → re-sync the Table's File Name to the file's actual post-rename title (Drive may append `(1)` on a collision). Migration of the classic "Google Drive receipts processing and analysis". Carried the same latent `DeterminismViolation` as `drive-invoice-to-xero` (identical `toIsoDate` helper) and would have failed on its first receipt; fixed in `019faae4` before any receipt reached it. **Still has had no live runs** — watch the first one. |
| [`drive-screenshot-to-xero-sales-invoice`](drive-screenshot-to-xero-sales-invoice/) | Durable | ✅ Enabled | Monthly "Notion Setup Sessions" activity screenshot added to the Drive **Notion Setup Session Invoices** folder → AI by Zapier extracts one line item per consultant/session-type combination → map session type to a fixed Xero item code → resolve the invoice period (majority value across rows) → create a **draft** Xero sales invoice billing **Notion Labs, Inc.** → rename the screenshot to `<invoice period> Notion Setup Sessions`. Migration of the classic Zap "Generate Setup Session Invoice". Unlike [`drive-invoice-to-xero`](drive-invoice-to-xero/), which resolves a *varying* vendor name read off a document, this Zap bills one *fixed* customer with a constant literal name, so it carries none of that Zap's vendor-contact-duplication risk. **A/B'd `standard/auto` against `advanced/auto` on three real screenshots before publishing**, per the repo's AI-tier convention — the two tiers agreed exactly on two of them, but on the third `standard/auto` silently dropped a real line (Ernest Choo, Calls Completed: 2), reproduced on a second run of the same file; `advanced/auto` caught it both times, so that's the tier shipped despite the extra task cost. Also caught, live, before publishing: `update_file_name`'s `file` input needs the plain Drive file ID, not the AI step's image-hydrate token — passing the hydrate token 404s. Tested end-to-end via `run-durable` against a real (already-processed) screenshot; both test-run draft invoices were deleted from Xero afterward so production starts clean. |
| [`drive-signed-agreement-to-notion`](drive-signed-agreement-to-notion/) | Durable | ✅ Enabled | Signed agreement PDF added to the Drive **Signed Agreements** folder → AI by Zapier extracts counterparty and signed date → rename the file → file it into the **Signed Legal Agreements** Notion data source with the Drive link and the file itself attached. Migration of the classic "Legal Agreement Summarizer", deliberately cut down: a separate Notion agent (triggered on new pages in the data source) extracts everything else — agreement type, dates, currency, amount, signatories, the AI summary. Fixes a real bug found in the live folder's already-processed files: the AI regularly misidentified **Company Flow Pte. Ltd. (our own entity) as the counterparty**, fixed with an explicit exclusion list for the company's own entity/names and an explicit empty-string rule for internal-only documents (salary revisions, board resolutions) — validated against 7 real documents via the offline `run-action` harness, no tier upgrade needed (`standard/auto` throughout). An Agreement Type classification step (first the classic Zap's narrow 4 options, then a consolidated 9-category taxonomy) was tried during development and then dropped entirely, since it only ever fed the file/page name rather than a Notion property. Verified live end-to-end against a real previously-unfiled document. The classic Zap's trigger polls the same folder but every downstream step is paused, so there's no duplicate-processing risk from leaving it running alongside this durable. |
| [`email-contact-page-zap`](email-contact-page-zap/) | Code step (classic Zap) | ✅ In production | [Sub-Zap] Retrieve Contact Page IDs for Email Addresses — single Code step replacing the original 24-node sub-Zap. |
| [`email-db-updates`](email-db-updates/) | Durable | ✅ Enabled | Notion Emails page added → poll for the mail block (via Notion MCP; not exposed by the public API) → parse From/To/Cc/MessageId → resolve Contacts through the email → page-id Zapier Table (AI-classify unknowns, create individuals with the Contacts default template, **named from the mail header's display name or, failing that, a high-confidence name the classifier reads out of the body** — a nameless contact can't reach `enrich-contact-records`' NinjaPear fallback) → patch the page (Contacts merged, Comment Access overwritten). Migration of the `notion-worker-email-db-updates` Notion Worker; the Emails DB automation is cut over to this workflow's catch URL, and the Worker remains deployed as rollback pending decommission. |
| [`enrich-contact-records`](enrich-contact-records/) | Durable | ✅ Enabled | Enrich Notion contact records with person profile data (Apollo first, then Lusha, then NinjaPear); collapses the old parent Zap + sub-Zap into one workflow. An enriched work address takes `Primary Email` when the existing Primary is a consumer mailbox; a corporate Primary is left alone. Contact name falls back to the page title, skip comments name each source's failure separately, and NinjaPear is never queried with a personal email (privacy-rejected — stripped from inputs). The company domain falls back to the Primary Email's host when the Company rollup gives none, and the NinjaPear call is gated on domain + name — the only input combination that actually resolves a profile (neither a work email nor a LinkedIn URL does, verified 2026-07-28). **Profile photos are uploaded into Notion, not linked (fixed 2026-08-12, version `019ff4a0`).** Path C used to set the page icon and cover to `{type:"external"}` pointing at the enrichment's photo URL — and both Apollo (`photo_url`) and NinjaPear (`profile_pic_url`) return LinkedIn CDN links signed with an expiry (`…?e=<unix>&t=<sig>`). Notion re-fetches an external icon on every view and keeps the dead link forever, so weeks later the page still *has* an icon and cover that **render as empty white space**, with no error and no alert anywhere. An audit found **210 of 962 contacts** already in that state, expiries reaching back to 2026-04-23. The photo now goes through `file_uploads` `mode:"external_url"` — Notion fetches it, stores it on its own S3 and re-signs the URL per read, landing as `{type:"file"}` that never expires; one upload backs both icon and cover. This is deliberately the **opposite** route to [`esignatures-status-to-notion`](esignatures-status-to-notion/), which must download the bytes because `external_url` probes with `HEAD` and its S3 links are presigned for `GET` alone; LinkedIn answers HEAD fine. A failed import is caught inside the step (no retry-loop spin), reported in the outcome comment as `Profile photo not stored: …`, and never fails the run — verified against a live expired URL. **The 210 existing pages cannot be repaired by rewriting URLs** (the links are dead and the bytes are gone); they need re-enrichment, which is currently blocked because **Apollo is out of lead credits** (`422 — You have insufficient credits!` as of 2026-08-12), leaving NinjaPear as the only photo source. `scripts/audit-contact-icon-urls.mjs` re-measures the damage on demand. **Also fixed 2026-08-12 (version `019ff4ab`): never send Lusha a name without a company.** `search_and_enrich_contacts` validates its *name* branch independently and a failure there rejects the whole request — `firstName` + `lastName` with no `company`/`domain` errors with `Each contact must have one of: id, linkedinUrl, email, or firstName + lastName + (companyName | companyDomain)` **even when `linkedinUrl` or `email` is also present**, and either identifies a contact on its own. Passing all five fields therefore hard-failed every contact on a consumer mailbox with no Company relation. The names now ride along only when a domain accompanies them, and empty values are omitted rather than sent as `""`. Measured against the live action: `linkedinUrl` alone resolves where `email` alone returns `Contact not found`. **Also 2026-08-12 (version `019ff5fa`): an enriched email is now gated on identity corroboration.** The address is the one enriched field that carries identity — it goes into `Primary Email`/`Secondary Email` and from there into `CONTACT_EMAIL_TABLE`, which is how the [Luma guest workflows](luma-guest-registered-to-event-attendance/) decide who a registration belongs to. Apollo's `people/match` is fuzzy and will match on a **name alone**: for a subcontractor contact with a personal Gmail Primary, no Company relation and no LinkedIn URL, it returned *a different person of the same name*, whose corporate address took the Primary slot via Path G-promote and was indexed into the Table. Everything downstream then behaved correctly on poisoned input — the stranger's Luma registrations, her account address, a company page, 25 email threads and a signed agreement all attached to the wrong contact, and **nothing errored**. An enriched address is now written only when something the CRM already holds corroborates it: a shared address, the contact's LinkedIn slug, the known company domain, a Lusha hit on inputs the contact owns (no name branch), or NinjaPear's domain+name resolution (the only way it resolves at all). An uncorroborated address goes nowhere — **not Primary, not Secondary, not the Table** (Path U) — and is named in the outcome comment for a person to judge; title, bio, city and photo still write, since they carry no identity and in this incident were correct. Known false negative: a contact on a personal mailbox with no Company and no LinkedIn whom Apollo matched genuinely by that address reads as uncorroborated (`reveal_personal_emails: false` means it isn't echoed back), so an enrichment is lost — visibly, in a comment, rather than silently writing a stranger into the identity oracle. Verified by 10 offline cases over the pure corroboration functions, including the exact regression. |
| [`gcal-event-updated-to-meeting-note`](gcal-event-updated-to-meeting-note/) | Durable | ✅ Enabled (cutover pending) | Google Calendar event updated → update the `Date` on its Notion meeting note. Update-only; never creates a note and touches nothing but `Date`. Migration of the classic "Updated Meeting → Update Meeting Note in Notion", **which had been silently doing nothing since May 2026**. That Zap matched `[Table] Meeting Note IDs`.`Event ID` against the trigger's event `id`, but the [`meeting-note-db-updates`](https://github.com/work-flowers/notion-worker-meeting-note-db-updates) Notion Worker had begun writing the **`iCalUID`** into that column — and `<id>@google.com` never equals `<id>`, so the "only continue if found" filter stopped every run with no error and no alert (92 of 849 rows). The iCalUID also identifies the *series*, not the occurrence, so the Worker's find-or-create kept overwriting one row's `Page ID`: a year of weekly standups collapsed to a single row, and daily meetings like `AI COE Daily Sync` to one row overwritten continuously since 14 May. **Both faults were fixed by re-keying the Table, not in workflow logic** — [`backfill-meeting-note-event-ids.mjs`](scripts/backfill-meeting-note-event-ids.mjs) moved 84 rows to the per-occurrence id and preserved every iCalUID in a new `iCal UID` (f9) column. `Event ID` (f3) is now an invariant: it holds the **occurrence** id, and anything writing this Table must respect that or the lookup silently returns nothing again. The lookup stays on the Table rather than a Notion search because Table ops are free, so the single task is spent only on a real reschedule. Improves on the classic Zap in two ways: the "did the time actually change?" guard compares **instants** rather than strings (Google sends `+08:00`, Tables stores `Z` — a string compare would rewrite Notion on every run), and a dead Notion page now **flags** the row `Archived` instead of *deleting* it, so a transient Notion error can no longer destroy a good mapping. `expand_recurring: false` matches the classic Zap: single-occurrence edits propagate, whole-series edits arrive as the master event and skip. Verified live 2026-07-31 across all five paths, including the reported failure (the 31 Jul standup went `03:45–04:15Z` → `08:00–08:30Z`). **Two cutover steps outstanding: turn the classic Zap off in the Zapier UI, and fix the Worker to write `event.id`** — the re-keying also repaired the classic Zap's lookup, so until it is off both fire on the same event, and if the Worker keeps writing iCalUIDs new notes go dead again. |
| [`gmail-attachments-to-drive-by-type`](gmail-attachments-to-drive-by-type/) | Durable | ✅ Enabled | Gmail email with PDF attachments → classify every attachment in **one** AI call → file each into Invoices / Paid Receipts / Signed Agreements / Financial Reporting. Migration of the classic "Gmail Attachments to Google Drive by Type" Zap. Triggers **per email, not per attachment**, so the classifier sees an invoice alongside the receipt that settles it — the Invoices folder now holds only bills that are still outstanding. Replaces the old "due date == invoice date" proxy with real payment evidence. **Since 2026-08-10, a paid invoice is treated as the receipt when no actual receipt exists**: an invoice carrying its own proof of settlement (paid markers on the document, or a sibling statement proving the recurring auto-charge) files to **Paid Receipts** instead of being dropped — it is the only record of the payment. When the email also carries a real receipt, the receipt remains the filed record and the invoice still skips, so the folder never gets two documents for one payment. Covered by 25 committed offline assertions (`decide.test.mjs`, `npm test`) over the real `decide()`. **Fixed `ZAP-26` on 2026-08-07 (`019fdb60`), and the lesson generalises to any durable that checkpoints upstream text.** Wise's password-protected *"Your monthly statement for Assets"* killed a run with `StepExhaustedError` naming `extract-text-0` — but extraction had worked. Files by Zapier returns a PDF's **raw bytes as "text"** when it cannot convert it and `failOnConversionError: false` stops it throwing, so the step returned 128 KB of binary carrying **101 NULs**; PostgreSQL rejects U+0000 in JSON with SQLSTATE `22P05`, the checkpoint runs *after* the step function returns so its own `try`/`catch` never saw it, and all 5 retries failed identically on byte-identical input. The Zap now detects undecoded file bytes (magic-number header, control-character or U+FFFD density) and takes its existing empty-text path, and scrubs NUL/C0/lone surrogates from anything leaving the step. The triage ticket's reading — a platform bug in `@zapier/zapier-durable@0.10.1`, triggered by zero-width non-joiners in the email body — did not hold up: U+200C is legal in Postgres JSON, and the body rides in the workflow *input*, which had already checkpointed cleanly. **Follow-up in `019fdb72` tightened the routing rule that failure exposed: an attachment whose text could not be extracted is now never filed at all**, whatever it classifies as, since classifying a document you cannot read is a guess off its filename and the surrounding email — and a guessed invoice in the Invoices folder is indistinguishable from a real bill. Unreadable attachments are also excluded from the sibling-evidence signals, so a filename-guessed "Receipt" can no longer suppress a genuine outstanding invoice. They are still classified and recorded, at no extra cost since the AI call is one per email. Accepted trade-off: a real unpaid bill arriving as a scan or an encrypted PDF stays in Gmail only — the fix for that is OCR on extraction, not relaxing the rule. See the directory README. |
| [`gws-new-user-provisioning`](gws-new-user-provisioning/) | Durable | ✅ Enabled | New Google Workspace user → find-or-create their row in the "Internal User IDs" Zapier Table (seeding First/Last Name from Google on create), invite them to the work.flowers Zapier team **unless the row already has a Zapier ID** (which [`internal-user-ids-to-table-and-notion`](internal-user-ids-to-table-and-notion/)'s Zapier deployment writes back when the invite is accepted), and send the welcome email with the Slack/Notion SSO join links. Migration of the classic "New Google Workspace Account -> Provision Standard Accounts"; cutover completed 2026-08-14 (classic Zap off, durable enabled by Dennis). Harvest/Linear API-provisioning is feasible but **explicitly deferred** — see the directory README's coverage audit. |
| [`internal-user-ids-to-table-and-notion`](internal-user-ids-to-table-and-notion/) | Durable ×5 | ✅ Enabled (Linear pending) | One shared `sync.ts` deployed five times — Slack / Harvest / Linear / Notion / Zapier Manager. Upserts the internal person's row in the "User IDs" Zapier Table, then mirrors every ID onto their row in Notion's native People data source. Port of the five classic "Add New \<System\> User ID" Zaps, plus the Notion mirror. **Linear's outgoing webhook still needs repointing at the new catch-hook** — see the directory README. |
| [`luma-event-to-notion`](luma-event-to-notion/) | Durable ×2 | ✅ Enabled | One code file deployed twice — `luma-event-created-to-notion` (`event_created`) and `luma-event-updated-to-notion` (`event_updated`). Upserts the Notion Events record keyed on Luma ID: properties, page cover, and description → page body (full replace). |
| [`luma-guest-registered-to-event-attendance`](luma-guest-registered-to-event-attendance/) | Durable | ✅ Enabled | Luma `guest_registered` → Event Attendance upsert. **Sole creator** of Event/Contact/Attendance records for the guest flow (see README for the race this prevents). A guest's `Work Email` registration answer becomes the contact's `Primary Email`, with the Luma account address kept as a Secondary. A ticked newsletter opt-in answer subscribes them in Buttondown under that same address — last, so the Buttondown → Contacts automation can't race this one into a duplicate contact. A registration that creates a **new** contact is flagged `Possible duplicate of` any name-equivalent existing contact, so an unindexed address can't quietly produce a second copy. **Fixed 2026-08-13 (version `019ff87b`): virtual check-ins were never ticked.** `checkedIn` only looked at `checked_in_at` (the physical QR-scan field) — a virtual guest joining the meeting link never gets a QR scan, so it's tracked in a separate `joined_at` field on the guest object that the code never read. Found via a live Notion Attendance record (ATT-273) whose Luma AI dashboard showed the guest joined a virtual event while Notion's `Checked In` stayed unticked; confirmed against real run history for that event where `joined_at` was populated but `checked_in_at` stayed null. `checkedIn` now also treats a non-null `joined_at` as checked-in. **Also 2026-08-13 (version `019ff886`): a checked-in guest now resolves `Approval Status` to `Attended`.** Despite its name that select is the record's *lifecycle* status — `Registered` and `Attended` are both options and neither is a Luma `approval_status` value — so attendance outranks approval state. It is also what stops `Attended` being clobbered: the sibling updater rewrites the select on every `guest_updated`, so mapping straight from `approval_status` would knock a checked-in guest back to `Approved` on their next profile edit. `checkedIn` is monotonic, so every later payload still resolves to `Attended` with no read-modify-write. |
| [`luma-guest-updated-to-event-attendance`](luma-guest-updated-to-event-attendance/) | Durable | ✅ Enabled | Luma `guest_updated` → pure updater: refreshes Approval Status / ticks Checked In on the existing Attendance record; never creates. Also applies the work-email → `Primary Email` promotion, which is the only path that catches a guest editing that answer after registering. **Fixed 2026-08-13 (version `019ff87b`): same virtual-check-in gap as the sibling registered workflow** — `checkedIn` now also treats a non-null `joined_at` as checked-in, not just `checked_in_at`, and a checked-in guest resolves `Approval Status` to `Attended` (version `019ff887`) — `resolveAttendanceStatus` is byte-identical in both guest workflows. |
| [`notion-company-to-linear-customer`](notion-company-to-linear-customer/) | Durable | ✅ Enabled | Notion Companies **polling** trigger (Deals relation changed) → create the company as a Linear customer (bare domain from `Website` + `COM-n` external id), customer id written back to the Company. Guards on the page's `Linear Customer ID` — note existing records hold *domains* there from the classic Zap's mis-mapping; the guard only asks "non-empty?", and the durable writes real UUIDs going forward. Migration of the classic "Company Linked to Deal -> Create Customer in Linear"; classic Zap disabled 2026-08-10. |
| [`proposal-fee-to-deal-value`](proposal-fee-to-deal-value/) | Durable | ⏸️ Disabled — retired | Proposal webhook from Notion → copy `Project Fee` onto the related Deal's `Value`. Migrated and retired the **same day** (2026-08-10): the migration's staleness flag — no visible data source carries `Project Fee` any more — turned out to be deliberate; Dennis had removed the property as superfluous. Never repointed to, never ran live; kept for reference. The classic "Update Deal Value When Proposal Fee Updated" Zap was disabled the same day. |
| [`notion-companies-to-zapier-table`](notion-companies-to-zapier-table/) | Durable | ✅ Enabled | Race-safe mirror of Notion Companies records into the company-ID Zapier Table, keyed on Notion Page ID. |
| [`register-zapier-partner-lead`](register-zapier-partner-lead/) | Durable | ✅ Enabled | Notion Companies "Register Lead" button → submit the company's Zapier account owner to the Zapier Solution Partner program as a managed-revenue client, index client id → Notion page id, and stamp the client id + `Submitted` back on the record. Migration of the classic "Register Zapier Lead" Zap. Adds a repeat-click guard (the partner tool would open a second service agreement), claims the longest agreement window the tool allows (`start + 1 year − 1 day`, up from `+11 months`), maps `company_size` from Notion's `Size`, and fixes the `Success`/`Status` columns the classic Zap wrote from response fields that don't exist. Cutover from the classic Zap completed 2026-08-07 (first real click that day; the old Zap is off). |
| [`zapier-partner-lead-status-to-notion`](zapier-partner-lead-status-to-notion/) | Durable | ✅ Enabled (cutover pending) | Zapier partner referral lead status change → write the lead's state onto its Notion Companies record: status, reason, 90-day expiry, converted date, commission rate, Y1/Y2 payout windows. Migration of the classic "Capture Zapier Lead Status Change" Zap, which resolved the company **only** via the lead Table and errored on a miss — so it did nothing for ~99% of leads (247 in the partner tool vs. 3 in the Table). Now resolves via the Table, then `Zapier Client Id`, then the lead's email through the contact Table, and backfills whatever it learns. Also added the missing `Converted`/`Expired` select options. **Adoption is gated:** a lead this system already tracks is written at any status, but an untracked one is only adopted into the CRM at `Converted` — the lead history is essentially one bulk event submission (45 of 45 sampled leads carry `source: mdfRequest…`, 78% have no Zapier account), and their shared 90-day expiry would otherwise adopt every attendee company in a single wave. **Turn the classic Zap off** — both are subscribed to the same trigger. |
| [`spot-project-request-to-notion`](spot-project-request-to-notion/) | Durable | ⚠️ Enabled; trigger can't deliver acceptances — covered by the hourly [sweep](spot-accepted-sweep-to-notion/) | **⚠️ 2026-08-24: the trigger does NOT deliver acceptances automatically.** The `updated_project_request` swap did not fix the "fires once, at `Pending`" problem — as deployed it fires once per request at creation and never on a later stage change (confirmed against a real acceptance, Commercial Laundry Solutions; filed by manual replay). Root cause is Zapier/SPOT-side trigger dedup, not the durable code — **confirmed by Zapier Product Escalations 2026-08-26 (ticket `9NPEKP-739EJ`): the poller dedupes on the stable `project_request_id`, and the `1.5.0 → 1.5.1` pin did NOT fix it.** Zapier then **merged/approved a fix** and shipped app **`1.5.2`**; the trigger is **re-pinned `1.5.1 → 1.5.2`** (via `zap.json` + the merge pipeline) to pick it up — unverified until the next real acceptance fires a run. The scheduled backstop [`spot-accepted-sweep-to-notion`](spot-accepted-sweep-to-notion/) is **live (hourly, dryRun-verified)** and files acceptances the trigger misses (the two coexist idempotently via the shared dedupe Table); it stays on as backstop until `@1.5.2` is verified firing, then it's retired. Project request **accepted** in the Zapier Solution Partner Operations Tool (an inbound brief from our partner-directory listing or a Zapier Sales match) → upsert a **Contact**, a **Company** and a **Deal** in the Notion CRM. **Trigger swapped 2026-08-18** to `updated_project_request` (stage change), gated to file **only at `Accepted`**: the original `new_project_request` trigger fired exactly once per request, at `Pending`, while SPOT still withheld the client's identity behind sentinel prose — so all three real requests (Doecke Electrical, USA Bath, Yanolja Cloud Solution) skipped correctly and the trigger stayed silent through every later stage change, including the one acceptance (Yanolja, 2026-08-18, filed by manual replay). That acceptance also proved an Accepted payload reveals the real email/names, answering the question that blocked the two-workflow split in [`two-trigger-design.md`](spot-project-request-to-notion/two-trigger-design.md) — now superseded by this simpler swap. Cutover completed 2026-08-18T12:14Z with the documented out-of-band `--trigger` publish (`01a014cb-165c-…`) — after the PR pipeline run raced the first attempt and had to be re-published over; the runbook in the directory README now records the ordering lesson. Third workflow on the same private partner app, running the opposite way to its two siblings: an inbound opportunity Zapier hands us rather than a lead we push. Company resolves mirror Table by domain → the contact's existing `Related Company` → a **live** Notion query by `Website` (that last one closes the create race the webhook-fed mirror Tables can't) → create. An existing Contact is only ever filled in, never overwritten; `Related Company` is unioned. The Deal opens at `Status: Lead` **from the Deals default template**, with `Type`, `Value` and `Expected Close` deliberately left empty for whoever scopes it. Consumer-mailbox requesters yield no domain, so nobody gets filed under a company called Gmail. **The first real request arrived 2026-08-06 and the trigger caught it in 57 seconds** (`02700000000002800hE`, run `019fd7e6-…`) — it **skipped without writing anything** (`operations: []`), because SPOT withholds client identity until a request is accepted and does so with **prose in the field** rather than an empty field: `email`/`first_name`/`last_name` each read *"Please accept or secure the lead to see the …"*. The skip was luck, not design — the email sentence fails the address regex, but the two name sentences are ordinary non-empty strings that would have become the Contact and Deal titles had the address ever parsed. That run also exposed **four missed key spellings** (`location`→country, `tool_requested`→apps, `service_requested`→services, `lead_stage`→stage) and **three value vocabularies** Notion does not share (`1-10`, `Retail & Wholesale`, and a *region* where a country was expected). All fixed and **published 2026-08-07 as version `019fd9ab-…`**, which round-trips byte-identical to the repo (54,643 bytes). The deeper consequence — that a `Pending` request has nothing to build a Contact from — is what the 2026-08-18 trigger swap resolves. **Built ahead of a Zapier-side change:** from ~2026-08-03 directory requests arrive via SPOT rather than as PartnerPage emails, so the trigger is right but empty until then. It was claimed *before* the cutover deliberately: a polling trigger banks its first poll as already-seen, so claiming afterwards risked swallowing the first real request as backlog. The **field set** is nonetheless evidenced, from the last PartnerPage delivery on 2026-07-31: it added a phone number, renamed the brief to `Comments`, and revealed that the requester's **Zapier account email is a separate field** — which a created Company now carries as `Account Owner Email Override`, making it immediately registerable via the `Register Lead` button. Only the SPOT key *spellings* remain inferred, so the raw payload is preserved three ways. Also note `Country` needs two vocabularies (Companies alpha-2, Contacts full names) or a select option gets minted by accident. **Only an accepted request is filed** (decided 2026-08-07, enforced by the stage gate since 2026-08-18): a request declined or expired in SPOT creates nothing in the CRM. Once filed, nothing here ever writes `Status` on an existing Deal, so a human's `Declined` on the Deal can't be undone. **The classic Zap covering this today, `Add Zapier Directory Leads to Contacts DB`, is broken** — it failed on the 2026-07-31 request with `Pipeline Stage is not a property that exists` (no such property on any of the three CRM data sources) and the request was silently dropped. That Zap has *not* been inspected: classic Zaps aren't exposed by the SDK CLI or the MCP connector, so the evidence is the Zapier alert email plus its absence from `list_workflows`. See the directory README. |
| [`spot-accepted-sweep-to-notion`](spot-accepted-sweep-to-notion/) | Durable | ✅ Enabled 2026-08-26 (`everyHour`, dryRun-verified) | **Scheduled reconciliation sweep** — the reliable backstop for [`spot-project-request-to-notion`](spot-project-request-to-notion/), whose polling trigger cannot deliver acceptances (Zapier dedupes it on the stable `project_request_id` — confirmed by Zapier Product Escalations 2026-08-26, ticket `9NPEKP-739EJ`, fix on Zapier's side with no timeline). On a `ScheduleCLIAPI@1.7.0` trigger it lists **every** project request via the SPOT `find_project_request` search action, keeps only the `Accepted` ones with a usable email, and files each that isn't already in the shared *SPOT Project Requests* dedupe Table (`01KYV1R8BWAZ697HQ8P1QV80AF`) — creating a **Contact**, **Company** and **Deal** with the *same* upsert logic as the trigger Zap, so a swept Deal is indistinguishable from a real-time one. The shared dedupe Table makes the two **safe to run side by side**: whichever files a request first wins, the other is a no-op — so if Zapier ever fixes the trigger, real-time filing resumes and this becomes a pure backstop, no coordination needed. Unlike the trigger Zap it **calls the partner tool**, so the SPOT connection is bound as a `--connections` alias (`spot`) alongside `notion_wf`. A `dryRun` input lists + classifies and writes nothing — the safe way to exercise it. **Cadence is the #1 review item:** hourly ≈ 24 `find_project_request` tasks/day; `everyDay` (~30/month) is the cost-sensible alternative once its param shape is verified. `workflow.ts` typechecks but was authored without a live run; **must be `run-durable`-tested before the pending-create merges** (merge = deploy = first publish, disabled). See the directory README. |
| [`stripe-payment-to-xero-invoice-paid`](stripe-payment-to-xero-invoice-paid/) | Durable | ✅ Enabled (cutover pending) | Stripe payment for a **Harvest-issued invoice** → record the payment against the matching sales invoice in Xero (bank account: Stripe SGD). Migration of the classic "Mark Harvest Invoice Paid in Xero when Stripe Payment Received". **Writes real money-adjacent records, so its design was discussed and confirmed rather than assumed:** the classic Zap had zero validation, so this durable adds an **idempotency guard** (looks the invoice up first, skips if already `PAID`/`AmountDue<=0`) — verified against real data, since invoice `HAR-13` is already paid with `Payment.Reference` exactly matching the Stripe payment intent id used in testing. A Harvest-tagged payment whose description or invoice number can't be resolved **throws** (a Zapier error alert) rather than silently doing nothing — confirmed deliberate. No currency/amount cross-check against the invoice was added, also confirmed, to stay close to the classic Zap's design. **Verified Stripe's `amount_received` needs no `/100` cents conversion** — Zapier's Stripe app exposes it pre-scaled to the real currency amount, cross-checked against the same already-paid `HAR-13` case (Stripe `900` = Xero's recorded `900.00`, not `9.00`). **The classic Zap's filter, formatter and Xero payment steps were ALL `paused: true`** — never actually recorded a payment. Every Harvest invoice currently in the live org is already paid, so no live write test was possible; the first real payment recorded will be the next Harvest invoice a client actually pays via Stripe. **Turn the classic Zap off**, don't unpause its steps. |
| [`notion-newsletter-to-buttondown`](notion-newsletter-to-buttondown/) | Durable | ✅ Enabled | Notion Newsletter Issues page → Buttondown draft/scheduled email, keyed on the page's Buttondown ID. On success it posts a confirmation comment on the triggering page (created/updated, schedule, link to the email), threaded into the discussion it started on the previous sync so re-syncs don't litter the page. |
| [`update-notion-when-rescheduled-in-buttondown`](update-notion-when-rescheduled-in-buttondown/) | Durable | ✅ Enabled (awaiting first real reschedule) | Buttondown send date set or changed → write it back onto the Notion **Newsletter Issues** page, resolved through the same `Email Newsletter ID Mapping` Table that [`notion-newsletter-to-buttondown`](notion-newsletter-to-buttondown/) writes. The reverse leg of that Zap. Migrated from the classic Zap via the Durables UI (2026-08-07), then fixed and republished into a fresh **account-visible** container the same day. **The fix that mattered: an unmapped email crashed it** — `find_record` returns `{data: []}` on a miss and the migrated code read `data[0].old.data.f1` unguarded, throwing `TypeError: … reading 'old'` and burning all five step retries (reproduced live, run `019fdb87-…`). Not an edge case: the trigger polls **every** scheduled Buttondown email, not only ones sent from Notion, so anything scheduled directly in Buttondown raised an error alert for an event that was never ours — exactly the alert-fatigue failure the repo's *skip, never throw* rule exists to prevent. It now logs and returns `{skipped: "no-notion-page-for-email"}`, while a payload with **no usable `email_id`/`publish_date` still throws**, naming the keys it got. Also added the rule 2 header, tightened the cast so the file typechecks under `--strict`, and made `InputSchema` actually validate instead of only feeding `z.infer`. **The container was recreated** because the migration tool made it `is_private: true` (violating rule 7) and visibility can't be changed after creation — cheap here because the trigger is a **poll**: no external catch URL to rewire. Old container `019fdb7e-…` is disabled with its trigger claim `released`, and the classic Zap is off. Verified end-to-end on the live Table and workspace: main path (writing back the `Send Date` the page already had, so a no-op), skip path, and the trigger's full nine-field payload. Two behaviours worth knowing: a durable trigger can't set a polling interval, so any custom interval on the classic Zap was silently dropped; and **unscheduling is invisible** — moving an email back to draft drops it from the `status=scheduled` list, produces no event, and leaves the Notion `Send Date` stale. No feedback loop with the outbound Zap, which fires only on a manual button press. |
| [`buttondown-email-sent-to-notion-newsletter`](buttondown-email-sent-to-notion-newsletter/) | Durable | ✅ Enabled | Buttondown email **sent** → set `Status = Sent` and stamp `Send Date` on the Notion **Newsletter Issues** page, resolved through the same `Email Newsletter ID Mapping` Table that [`notion-newsletter-to-buttondown`](notion-newsletter-to-buttondown/) writes. The third leg of the newsletter loop, alongside that Zap (outbound) and [`update-notion-when-rescheduled-in-buttondown`](update-notion-when-rescheduled-in-buttondown/) (reschedule). Migrated from the classic "Newsletter Sent -> Update Status in Notion". Carries the reschedule Zap's hard-won lesson from the start: the trigger fires for **every** email Buttondown sends, not only ones drafted from Notion, so an unmapped id logs and returns `{skipped: "no-notion-page-for-email"}` rather than throwing and burning five step retries on an event that was never ours. `publish_date` is now optional, so a send without one still marks the issue `Sent` instead of writing an empty date; a payload with no email id still throws, naming the keys it got. No conflict with the reschedule Zap — both write `Send Date` from the same source at different moments, so the later write is a no-op, and only this one touches `Status`. Verified on the live Table and workspace against an issue that was already `Sent` with that exact date, so the main-path test changed nothing. Poll only, no catch URL, so there was nothing external to rewire; the classic Zap was turned off on 2026-08-10 (confirmed by Dennis, not machine-verifiable). |
| [`buttondown-unsubscribe-to-notion-contact`](buttondown-unsubscribe-to-notion-contact/) | Durable | ✅ Enabled | Buttondown unsubscribe → untick `Mailing List` on the matching Notion contact. The inbound mirror of [`new-buttondown-subscriber-update-in-notion`](new-buttondown-subscriber-update-in-notion/), resolving the contact the same way: through the shared email→page-id Zapier Table rather than a Notion query. Migrated from the classic "Unsubscribe in Buttondown -> Update in Notion". Runs on the private **Buttondown Unofficial #2** app's `new_unsubscribe` trigger, because the official app's `unsubscriber` read is `is_hidden` and therefore off the stable surface. Two fixes the classic Zap needed: an unmapped address now **skips rather than throws** (Buttondown holds plenty of addresses that were never CRM contacts — site sign-ups, imported lists), and the lookup is **lowercased** to match how [`contact-emails-to-zapier-table`](contact-emails-to-zapier-table/) indexes the Table, without which a mixed-case unsubscribe missed every row. Verified live against a contact whose `Mailing List` was already false, so the main-path test wrote the value it found. Poll only, no catch URL, so there was nothing external to rewire; the classic Zap was turned off on 2026-08-10 (confirmed by Dennis, not machine-verifiable). |
| [`deal-won-to-newsletter-subscriber`](deal-won-to-newsletter-subscriber/) | Durable | ✅ Enabled | Notion **Deal Won** automation → subscribe the deal's contact email(s) to the Buttondown newsletter. Migrated from the classic "Deal Won -> Add Lead Contact to Mailing List". **Uncovered a live failure mode worth knowing across the repo: Buttondown answers a re-subscribe of an existing address with `This subscriber was blocked by your firewall`** — a `partner_error` at HTTP 200, not an "already exists" message, and indistinguishable from a genuine firewall refusal of a brand-new address. Reproduced live (run `019febef-133f-…`), where it burned all five step retries and failed the run; a re-won deal would have alerted. Fixed by **looking before writing** — `search subscriber` first (a miss returns `{data: []}`, not an error), short-circuit if found — rather than catching the message, which would also have swallowed a real refusal and left a lead silently unsubscribed. Also re-reads the deal page instead of trusting the webhook snapshot (`Contact Email` is a **rollup**, computed downstream of the very relation that triggered the automation), subscribes **every** distinct address in the rollup where the classic Zap flattened them into one unusable comma-joined string, and skips — never throws — on an empty payload, an empty rollup, or an archived deal. **Cutover done 2026-08-10** — the Notion Deals automation posts to this durable's catch URL (`https://hooks.zapier.com/hooks/catch/20495893/CqdlciNMGmX51pfG/`) and the classic Zap is off, so this is the code that runs. Its first traffic was the wiring-up test click delivering an empty body, which returned `{skipped: "empty-payload"}` instead of raising — the guard working in production. No real won-deal event has reached it yet. The create branch is untested — every address reachable from a real won deal is already subscribed, and exercising it would have added a real person to the live list. |
| [`notion-contact-to-newsletter-subscriber`](notion-contact-to-newsletter-subscriber/) | Durable | ✅ Enabled | Notion **Contacts** button press → subscribe that contact's `Primary Email` to the Buttondown newsletter. Migrated from the classic "Click to Add Newsletter Subscriber". Same Buttondown firewall finding and same look-before-you-write fix as its sibling [`deal-won-to-newsletter-subscriber`](deal-won-to-newsletter-subscriber/) — here it makes a second button click idempotent instead of a hard error alert. The empty-payload guard earns its keep more than anywhere else in the repo: a Notion **button property** delivers an empty body on every test click while it is being wired up, so throwing on that would turn routine setup into error alerts for events that never existed. Re-reads the contact page rather than trusting the click's snapshot, and skips on an empty `Primary Email` or an archived page. Writes nothing back to Notion — the `Mailing List` tick returns on its own via [`new-buttondown-subscriber-update-in-notion`](new-buttondown-subscriber-update-in-notion/) once Buttondown confirms. **Cutover done 2026-08-10** — the Contacts button property posts to this durable's catch URL (`https://hooks.zapier.com/hooks/catch/20495893/mu4Zr5AmKVd2g2Zv/`) and the classic Zap is off, so this is the code that runs on a click. Its first traffic was exactly the case the guard exists for: the wiring-up test click delivered an empty body and returned `{skipped: "empty-payload"}` rather than an error alert. No real press on a populated contact has reached it yet. The create branch is untested, for the same reason as its sibling. |
| [`xero-invoice-paid-to-gmail-confirmation`](xero-invoice-paid-to-gmail-confirmation/) | Durable | ✅ Enabled (cutover pending) | Xero sales invoice marked paid → re-read the invoice and, **only if it is settled in full**, email the customer a payment confirmation (bcc Dennis). Migration of the classic "Send Gmail payment confirmation when invoice paid in Xero", which was **entirely paused** and so had never sent one. The guard is the point: the classic Zap went straight from trigger to send, so a part-payment would have produced "Thank you for your payment!" quoting only the part paid, and then nothing when the balance was settled. Also sets the trigger's `trigger_preference: detailed` (Zapier's own help says to, when filtering on a change type — the classic Zap left it at "asap", so its `change: paid` filter was read against Xero History that may not have caught up), builds a real recipient list from the contact **and** its contact persons honouring Xero's `IncludeInEmails` flag, and renders dates as `2026-07-16` rather than `2026-07-16T00:00:00`. **The only Zap here that emails customers with no human in the loop** — turn the classic Zap off, don't unpause it. |
| [`xero-overdue-invoice-to-gmail-reminder`](xero-overdue-invoice-to-gmail-reminder/) | Durable | ⏸️ Disabled | **Retired 2026-08-06 — Xero's native invoice email reminders do this job instead.** Switched off because it was one of five Xero *polling* triggers that together exhausted Xero's 5,000-calls/day-per-tenant limit, taking every Xero Zap in the workspace down for ~10 hours. A durable poller can't be rate-limited (no interval field in the `--trigger` payload) and this trigger has no webhook variant, so retiring it was the only lever. Disabled, not deleted — the version is intact. What it used to do: Xero sales invoice 1 day overdue → re-read it for the online-invoice URL and PDF and, **only if it is still awaiting payment**, create a Gmail **draft** reminder with the invoice attached. Never sends — a person clicks send, as the classic "Overdue Invoice Reminders (1 Day)" also did. Fixes three real faults in it: the PDF attachment pointed at `gives[trigger].InvoicePDF` while the lookup was set to `include_pdf: false`, so **the reminder never carried the invoice**; the body read "the following invoice from `{{Contact.Name}}`", interpolating the *customer's own* name and telling them their invoice came from themselves; and nothing checked the invoice was still owed, so one paid between the poll and the run would still have been chased. Its trigger was live but both action steps were paused, so it polled and did nothing — turn it off, don't unpause it. |
| [`xero-bank-transactions-to-zapier-table`](xero-bank-transactions-to-zapier-table/) | Durable | ✅ Enabled | **Hourly schedule** → read Xero bank transactions dated within `[newest mirrored row − 7d, today]` → **find-or-create/refresh** each row in the `Xero Bank Transactions` Zapier Table, keyed on transaction id. Migration of the classic "Process New Reconciled Transactions", which created unconditionally (the Table holds 4 duplicate pairs). Adds race-safe dedupe of concurrent creates, refreshes a row when a mirrored field actually changed (`reference`, `currency_rate` and `has_attachments` are all filled in by Xero *after* a transaction is created), and maps the previously-unmapped `currency_rate`. **Load-bearing for [`drive-invoice-to-xero`](drive-invoice-to-xero/)** — while this Zap's write step sat paused the Table fell ~5 days behind and that Zap silently raised duplicate draft bills. **Re-pointed from Xero's `bank_transaction` polling trigger to `ScheduleCLIAPI` / `everyHour` in `019fd4b2` (2026-08-06), cutting its Xero cost from ~1,440 API calls/day to ~24–48.** **⚠️ That first schedule version silently wiped the `date` column on 214 of 685 rows and was fixed in `019fdb43` (2026-08-07); all 214 are repaired and the mirror is verified converged (1 Xero page, 7-day window, 0 churn).** Its `toIsoDate` accepted only `YYYY-MM-DD` while Xero's raw API returns `Date` in .NET epoch form and only `DateString` as ISO — and since `Date` is always present, `??` never fell through, so every row got `date: null` and the update path wrote that null over the good stored value. The corruption was self-amplifying, because this Zap derives its read window from the newest mirrored row. The durable fix is a new `wouldBlankAPopulatedField` guard — a blank extracted value can no longer overwrite a populated stored one, enforced in both the diff *and* the write payload — plus a `dryRun` input, because the pre-publish test had died at the Xero fetch and never executed the extraction code at all. That was not an optimisation but an incident fix: five Xero pollers on one tenant exhausted Xero's **5,000-calls/day-per-tenant** limit, taking every Xero Zap in the workspace down for ~10 hours (`x-rate-limit-problem: day`, `x-daylimit-remaining: 0`, with `x-minlimit-remaining: 60` proving it was a daily-quota exhaustion and not a burst). A durable polling trigger **cannot be rate-limited** — the `--trigger` payload has no interval field, verified across all 43 live triggers — so the classic Zap's `polling_interval_override: 5` was silently dropped at migration; a schedule trigger is the knob that doesn't otherwise exist. The 7-day overlap also made the sync **self-healing**, which retires two documented gaps: a missed run or a disabled hour is simply re-read on the next fire (the polling trigger primed its dedupe on first poll, so a gap was permanent), and recent rows are re-read hourly so the stale-column drift above finally gets corrected. No new state table was needed — the mirror Table *is* the dedupe state. The historical 2026-07-21..26 gap predates the change and stays unfilled, deliberately. |
| [`xero-bill-approved-to-subcontractor-email`](xero-bill-approved-to-subcontractor-email/) | Durable | ⏸️ Disabled | **Retired 2026-08-06 — superseded by [`xero-invoice-alerts`](xero-invoice-alerts/), where this runs as the `email-subcontractor` channel with its message text and filters ported verbatim.** Switched off because its Xero *polling* trigger cost ~1,440 API calls/day and five such pollers exhausted Xero's 5,000/day-per-tenant limit, downing every Xero Zap for ~10 hours. A durable poller can't be rate-limited (no interval field in `--trigger`), so removing pollers was the only lever. **Do not re-enable** — it would double-alert *and* restore the quota cost. What it used to do: Xero bill reaching **AUTHORISED** with a line item coded to the **Subcontractor Fees** account (`490`) → email the vendor a confirmation (invoice number, amount due, due date) with their own uploaded invoice/receipt attached. Migration of the classic "Confirmation Email for Sub-contractor Invoices". No Xero re-read — the `bill` trigger already delivers `Contact`, `LineItems` and `attachments`. **Tightens the account-code filter from `icontains` to an exact match** (the classic Zap's substring match on `"490"` would also fire on a hypothetical `"4900"` or `"1490"`), and **moves date formatting off a paid Zapier Formatter step onto free pure-code integer date arithmetic**. **The classic Zap's filter, formatter and Gmail steps were ALL `paused: true`** — only its trigger was live, so it never actually sent a confirmation; this durable is the first time this email goes out for real. Three bills are already AUTHORISED with a `490` line item as of publish (Ernest Choo, Lantern Labs, Nasri Nasir) — none get a retroactive confirmation, since a polling trigger primes its dedupe on first poll. Verified via `dryRun: true` against the real Lantern Labs `INV-26-0007` bill data. **Turn the classic Zap off**, don't unpause its steps. |
| [`xero-bill-approved-to-wise-transfer`](xero-bill-approved-to-wise-transfer/) | Design record | 🗄️ Shelved — never deployed | Xero bill reaching **AUTHORISED** ("Awaiting Payment") → read the vendor's row in the `Vendor Payment Details` Table → reuse or create the Wise recipient → quote → create an **UNFUNDED** Wise transfer, and stop. A human funds it in Wise; nothing here moves money. Wise has no first-party Zapier app, so every call goes through the API by Zapier connection *Wise - workFlowers* (`App235435CLIAPI`, business profile `80913588`). Keyed for idempotency on the Xero `InvoiceID`, which is already a GUID and is handed to Wise verbatim as `customerTransactionId`, so the free Table guard and Wise's own dedupe key are the same identifier. `details.reference` carries the invoice number so the payout lands on the Xero bank feed already matchable — which is why nothing writes a Xero Payment back. Every guard is free: wrong bill type, nothing due, no vendor row, a `needs_review` flag, a non-transfer payment method, a currency mismatch or an unbuildable corridor all return at **0 tasks**; a prepared transfer costs 2, or 3 the first time a vendor needs a recipient. **🗄️ Shelved on 2026-08-12 and never deployed — do not treat this as pending work.** It was built as a `wise-transfer` channel of [`xero-invoice-alerts`](xero-invoice-alerts/) (PR #87) to avoid re-adding the Xero poller that helped exhaust the tenant's daily quota on 2026-08-06, reviewed, and then reverted in full at Dennis's decision to **wait for Wise's own first-party sync of Xero bills into Wise** rather than have this repo own a payment rail. No version of `xero-invoice-alerts` ever contained it. The directory keeps the design record only. Design, verified Wise facts, and what the build learned — including the `email` recipient type that needs no bank details, and the claim-row latch bug: [`README.md`](xero-bill-approved-to-wise-transfer/) and [`payment-details-design.md`](xero-bill-approved-to-wise-transfer/payment-details-design.md). |
| [`xero-contact-from-notion-deal`](xero-contact-from-notion-deal/) | Durable | ✅ Enabled | Notion **Company** or **Deal** webhook → create that company's contact in **Xero**, named after the company with its Notion `ID` (`COM-nnn`) as the Account Number. Fired from two places: the `Create Xero Contact` **button** on Companies and a **stage automation** on Deals. Migration of the classic "Create Xero Contact from Notion", which understood only the Company payload shape — this one detects the page's data source and resolves the Company/Deal pair either way. **Closes a hole in the classic Zap's branch:** Path A required billing contact ≠ deal contact and Path C required *no* billing contact, with no Path B, so "billing contact **is** the deal contact" and "billing contact but no deal contact" matched no path and the Zap silently did nothing — Knoxx Business Group hits the first case today. Both now produce a single-person contact; a genuinely different deal contact still rides along as Xero's secondary contact person. **The dedupe guard now has teeth:** the classic Zap stopped when `[Table] Company IDs` held a `Xero Contact ID`, but nothing wrote that property, and Xero's Create/Update Contact matches on **name** — so a repeat trigger quietly *overwrote* the live contact's people rather than being skipped. The durable writes the new id back onto the Notion company, which [`notion-companies-to-zapier-table`](notion-companies-to-zapier-table/) mirrors into the Table for free, and reads the guard off the company page it already fetched rather than the staler Table. Dry-run tested (`previewOnly`, Notion reads only) across all three shapes against live CRM records; testing must use that flag, since a live run upserts a real Xero contact by name. **Cutover completed 2026-08-02:** the Notion button and the Deals automation were repointed at the new catch URL and the classic Zap was disabled, so this durable is the only thing serving those two senders. During that repointing a bare ping of the catch URL (`{"querystring":{}}`, no body) produced a failed run — a public catch URL gets opened in browsers and "test"-fired. An empty payload now returns `skipped: "empty-payload"` instead, while a payload carrying content but no page id still fails loudly, since that is a real event we failed to understand. |
| [`xero-contact-to-notion-company`](xero-contact-to-notion-company/) | Durable | ✅ Enabled | **Hourly schedule** → `GET /Contacts where UpdatedDateUTC >= today−7d` → for any Xero contact carrying a Notion Company ID in its **Account Number**, write that contact's `ContactID` onto the matching Notion Company page. The **Xero → Notion** half of the company/contact link; [`xero-contact-from-notion-deal`](xero-contact-from-notion-deal/) covers Notion → Xero. Exists for the case that one structurally cannot cover: a contact made by hand in Xero (typically a transaction from a new vendor) is tied to an existing Notion company by pasting its Notion Company ID into Account Number — nothing changes on the Notion side, so no webhook fires. **Replaces two functionally identical classic Zaps** which cost ~2,880 Xero calls/day between them at 1-minute polling and were the largest single contributor to the 2026-08-06 quota exhaustion. It also **fixes their write target**: they wrote the id into `[Table] Company IDs` (`f15`) and left the Notion property empty, which made the link *temporary* — the Notion→Table mirror clears `f15` when Notion is empty — and left the sibling durable's dedupe guard blind, so a later trigger would re-run Xero's name-matched upsert and silently overwrite the hand-made contact's people. Notion is now the authoritative write and the mirror carries it to the Table. The Table lookup stays, because it is the **only** source of the Notion page UUID (Account Number holds `COM-766`, not a UUID). A *changed* link is never applied — it reports a conflict rather than repointing. `--input '{"dryRun":true}'` inspects a full pass without writing. **The two classic Zaps must be turned off in the Zapier UI.** |
| [`xero-draft-bill-to-slack-alert`](xero-draft-bill-to-slack-alert/) | Durable | ⏸️ Disabled | **Retired 2026-08-06 — superseded by [`xero-invoice-alerts`](xero-invoice-alerts/), where this runs as the `slack-bill-draft` channel with its message text and filters ported verbatim.** Switched off because its Xero *polling* trigger cost ~1,440 API calls/day and five such pollers exhausted Xero's 5,000/day-per-tenant limit, downing every Xero Zap for ~10 hours. A durable poller can't be rate-limited (no interval field in `--trigger`), so removing pollers was the only lever. **Do not re-enable** — it would double-alert *and* restore the quota cost. What it used to do: Xero new **draft** bill → post an alert to the **#finance** Slack channel with vendor, amount, due date, invoice number and a link to the bill in Xero. Migration of the classic "New Draft Bill Alert". No Xero re-read — the `bill` trigger already delivers the full invoice. Formatting fixes over the classic Zap: amount is two-decimal and thousands-separated, due date is normalized to `YYYY-MM-DD` regardless of which of Xero's two date shapes the trigger hands back, and a bill with no invoice number (an absent key, not `null`) renders `(not numbered)` instead of a blank line. **The classic Zap's Slack step was `paused: true`** — only its trigger was live, so it never actually posted an alert; this durable is the first time this notification goes out for real. No draft bills existed in the live org at build time, so it was verified with a synthetic payload via a manual `dryRun: true` run rather than a live trigger delivery — watch the first real one. **Turn the classic Zap off**, don't unpause its Slack step. |
| [`xero-invoice-alerts`](xero-invoice-alerts/) | Durable | ✅ Enabled | **Hourly schedule** → `GET /Invoices where UpdatedDateUTC >= today−7d` → classify each invoice and alert on qualifying **status changes**, deduped through its own state Table. `ACCPAY`+`DRAFT` → Slack **#finance**; `ACCPAY`+`AUTHORISED` with a Subcontractor Fees (`490`) line item and a valid email → approval email to the vendor; `ACCREC`+`DRAFT` → Slack #finance with per-item line items. Everything else — `PAID`, `VOIDED`, `SUBMITTED`, authorised sales invoices — is ignored. **Replaces three Xero polling Zaps** ([`xero-draft-bill-to-slack-alert`](xero-draft-bill-to-slack-alert/), [`xero-bill-approved-to-subcontractor-email`](xero-bill-approved-to-subcontractor-email/), [`xero-draft-sales-invoice-to-slack-alert`](xero-draft-sales-invoice-to-slack-alert/)), cutting **~4,320 Xero API calls/day to ~24** — the fix for the incident in which five Xero pollers exhausted Xero's 5,000/day-per-tenant limit and downed every Xero Zap for ~10 hours. A durable poller **cannot be rate-limited** (no interval field in `--trigger`, verified across all 43 live triggers), so a schedule trigger is the only available knob. **The window is on `UpdatedDateUTC`, not `Date`** — a bill issued weeks ago and approved today keeps its original `Date`, so a `Date` window would never see the approval. The two `bill` pollers could **not** simply be merged: each polling subscription keeps its own dedupe store, which is the only reason draft→authorised was detectable, so one unfiltered poller would have deduped the invoice on creation and silently lost every approval. Dedupe is now deliberate, via Table `01KZACA2ZA3XJWWGSMNEC381ZE` (created through the **SDK**, which validates, not the Tables actions) keyed on `last_alerted_status`. Safeguards: a **priming** first run records state and sends nothing (mirroring a poller's first poll), a failed state read throws rather than being mistaken for empty, and `MAX_ALERTS_PER_RUN` caps blast radius if the table is ever emptied. One deliberate change from the retired Zaps — the subcontractor email is **text-only**, because the vendor's uploaded attachment was only reachable via trigger-payload hydrate refs and Xero exposes no attachment *read* action; that Zap had never actually sent, so nothing regressed. `--input '{"dryRun":true}'` inspects a full pass without sending or writing. |
| [`xero-draft-sales-invoice-to-slack-alert`](xero-draft-sales-invoice-to-slack-alert/) | Durable | ⏸️ Disabled | **Retired 2026-08-06 — superseded by [`xero-invoice-alerts`](xero-invoice-alerts/), where this runs as the `slack-sales-draft` channel with its message text and filters ported verbatim.** Switched off because its Xero *polling* trigger cost ~1,440 API calls/day and five such pollers exhausted Xero's 5,000/day-per-tenant limit, downing every Xero Zap for ~10 hours. A durable poller can't be rate-limited (no interval field in `--trigger`), so removing pollers was the only lever. **Do not re-enable** — it would double-alert *and* restore the quota cost. What it used to do: Xero new **draft sales invoice** → post an alert to the **#finance** Slack channel with invoice number, customer, dates, status, amount due, total tax and a per-line-item breakdown. Migration of the classic "Draft Sales Invoice Alert"; sibling to `xero-draft-bill-to-slack-alert` above. No Xero re-read — the `sales_invoice_v2` trigger already delivers the full invoice including `LineItems`. **Fixes a line-item array-flattening bug:** the classic template used `{{LineItems[]Description}}`-style tokens, which Zapier renders as one list per *field* (all descriptions, then all quantities, then all amounts) rather than one line per *item* — any invoice with more than one line item read as three disjoint lists. This durable keeps each item's description/quantity/amount together. Amounts and dates are also normalized. **The classic Zap's Slack step was `paused: true`** — never actually posted. No draft sales invoices existed in the live org at build time, so it was verified with a synthetic multi-line-item payload via a manual `dryRun: true` run — watch the first real one, especially the line-item formatting. **Turn the classic Zap off**, don't unpause its Slack step. |
| [`start-a-timer-from-notion-task`](start-a-timer-from-notion-task/) | Durable | ✅ Enabled | Notion Tasks "Track Time" button → start a Harvest timer on the project mapped to the task's Notion Project; a second click the same day restarts the existing entry instead of duplicating it. Migrated from the classic Zap of the same name via the Durables UI — the migration output was non-functional (didn't parse; four Zapier template expressions left as literal strings; two step outputs mis-bound to `input`; trigger unclaimed) and was rewritten before publishing. Cutover completed 2026-07-27: the Notion Tasks DB automation posts to the catch URL. |
| [`new-buttondown-subscriber-update-in-notion`](new-buttondown-subscriber-update-in-notion/) | Durable | ✅ Enabled | Buttondown confirmed-subscriber → Notion Contacts upsert (ticks `Mailing List` on an existing contact, or creates one with the Contacts default template). Migrated from the classic Zap via the Durables UI, then had its raw Notion OR-filter lookup swapped for the shared email→page-id Zapier Table, matching how every other contact-resolution Zap in this repo works. |
| [`whatsapp-slack-bridge`](whatsapp-slack-bridge/) | Durable ×3 | ✅ Enabled | Two-way WhatsApp Business ↔ Slack bridge, one `#whatsapp-<name>` channel per contact. Three durables sharing one `shared.ts` — `whatsapp-message-to-slack`, `slack-reply-to-whatsapp`, `whatsapp-message-status` — replacing the classic "WhatsApp -> Slack", "Slack Replies -> WhatsApp" and "Message Delivery". Fixes, all evidenced from the live Tables: **36 of 42 contacts could never receive a message** (a channel was only created when the contact record was brand new); **a new contact's first message was lost** (channel creation and posting were concurrent sibling paths racing each other); **concurrent find-or-create duplicated contacts** (42 rows for 34 phones — `6591520097` got five inside 0.54s); outbound rows keyed on `.id` while writing `messages[]id`, so the lookup ran empty; **every public Slack message outside a `whatsapp-*` channel raised a Zap error**, now a free early `return`; status could regress (`delivered` clobbering `read`) and is now monotonic; `WhatsApp Username` received the sender's own *phone number* while the trigger's real `contact_name` was thrown away, and `First Name` was never written at all; opt-out overwrote the `Marketing Opt-In Date`/`Source` columns, destroying consent provenance; media was lossy both ways (voice notes, documents, video and location all arrived as "No Text"; outbound dropped every non-PDF non-image and sent only the first attachment); and a failed send told nobody — it now threads onto the Slack message that caused it, which needed the Slack `ts` stored on outbound rows for the first time. Adds a 24-hour-window guard that warns in-thread instead of failing at Meta. **Cutover completed 2026-07-29** — the three classic Zaps are off and all three durables are enabled with their triggers `active`; it had to be a deliberate flip rather than a parallel run, because the outbound durable and its classic Zap both subscribe to every public Slack message and would have sent every reply to the customer twice. Nothing needed repointing — all three are Zapier-managed app triggers. **Live media validation is still outstanding:** inbound `document.file` / `audio.file` / `video.file` field names are read defensively but unproven, so watch the first real voice note and PDF. The classic "Send Template Message" Zap is out of scope and still live; it reads `WhatsApp Username`, so it starts greeting people by name for free, but its **MARKETING**-category `reeng` template still ignores `WhatsApp Marketing Opt-In` (4 of 6 channel-having contacts are opted out). |
| [`esignatures-send-for-signing`](esignatures-send-for-signing/) | Durable ×2 | ✅ Enabled | Notion **Send for signing** button → an eSignatures draft contract built from the record's own page body. One shared `shared.ts` deployed twice — `sow-send-for-signing` (SOWs) and `project-addendum-send-for-signing` (Project Addendums) — replacing the classic "Send SOW for Signing" and "Send Project Addendum for Signing". The classic page-to-markdown step **could not be ported at all**: it used `ae:577300`, a hidden Zapier *action extension* that `runAction` refuses outright, so it is replaced by Notion's native `GET /v1/pages/{id}/markdown` — which is also more faithful than the `block_children` converter that extension wrapped. Fixes a real bug: the addendum flow generated addenda from the **SOW template**, and now uses the "Consultant Project Addendum" template (both shells share the `{{contract-body}}` placeholder, so it was one constant). The markdown normaliser is adapted from [`notion-newsletter-to-buttondown`](notion-newsletter-to-buttondown/) and needed two contract-specific fixes found in testing, both of which would have mangled every real agreement: Notion's `<table>` HTML was being **shredded into one block per tag** by the inherited block-separation pass (now converted to Markdown pipe tables), and the inherited tab-strip **flattened nested list items — promoting a sub-clause to a clause**, which changes what the agreement says (tabs on list items now become Markdown's 4-space nesting, and tabs under a blockquote carry the `> ` down so a callout keeps its body). `test` is set to `false` explicitly because that field is *required* with a default of `"true"`, which would otherwise make every run a throwaway test contract. Verified end-to-end against purpose-made scratch records (every real mapped record is a live client agreement), including override-email precedence over the signatory rollup and retry idempotency on the mapping row. **Cutover done 2026-08-04** — both Notion buttons now post to these durables' catch URLs, so this is the code that runs on a click; leaving the classic Zaps enabled does not double-draft, since each button posts to one URL and theirs no longer fires. Remaining: turn the two classic Zaps off, delete the three test drafts left in eSignatures, and confirm on the first real draft that eSignatures substitutes `{{contract-body}}` as expected — the one thing unverifiable from here, since the app exposes no Get-Contract action. |
| [`notion-page-deleted-to-zapier-tables`](notion-page-deleted-to-zapier-tables/) | Durable | ✅ Enabled (cutover pending) | Notion `page.deleted` webhook → remove or flag the page's row in the Zapier Table that maps it: **Companies** rows deleted from `[Table] Company IDs`, **Meeting Notes** rows flagged `Archived` in `[Table] Meeting Note IDs` (never deleted — same rule as [`gcal-event-updated-to-meeting-note`](gcal-event-updated-to-meeting-note/)), **[Ernest] Internal Setup Calls** rows deleted from `[Table] Notion Setup Session Mapping`. Migration of paths B/C/D of the classic "Delete Contact, Company or Meeting Note Page from Zapier Table" — path A (Contacts) was already retired into [`contact-emails-to-zapier-table`](contact-emails-to-zapier-table/), and Contacts events skip here for that reason. Improves on the classic Zap by handling `page.undeleted`: a restored meeting note is un-flagged, and a restored company is forwarded to [`notion-companies-to-zapier-table`](notion-companies-to-zapier-table/)'s catch URL so the mirror re-creates the row (a restore fires no DB automation, so the mirror would never hear about it otherwise). Zero tasks per run — every branch is a free Tables call, no connections. Verified live with scratch rows across all seven paths. **Cutover pending: repoint the Notion integration-webhook subscription at the new catch URL and turn the classic Zap off.** |
| [`sow-signed-to-deal-won`](sow-signed-to-deal-won/) | Durable | ✅ Enabled (cutover pending) | Notion SOWs automation (Status → Signed) → re-read the SOW page and set its related Deal's `Status` to **Closed Won**. The last hop of the deal lifecycle: [`esignatures-send-for-signing`](esignatures-send-for-signing/) sets *In signing* when the SOW goes out; this closes the Deal when it is signed. Migration of the classic "SOW Signed -> Mark Deal as Won". Re-reads the page rather than trusting the webhook snapshot, and skips — never throws — on empty pings, archived SOWs, non-SOW pages and SOWs with no Deal relation (parity with the classic filter). Verified live on the Terrascope SOW (deal already Closed Won, so the write was idempotent) plus both skip paths. **Cutover pending: repoint the Notion SOWs automation and turn the classic Zap off.** |
| [`contact-us-form-to-notion-contact`](contact-us-form-to-notion-contact/) | Durable | ✅ Enabled (cutover pending — urgent) | **Contact Us** form submission (Zapier Forms `new_form_submission`) → upsert the person in Notion **Contacts**. Migration of the classic "When a new Contact Us form submission is created, save to Notion", upgraded from that Zap's blind create — the thing that feeds the duplicate-merge loop — to the repo's standard resolve-through-the-email-Table pattern (as in [`spot-project-request-to-notion`](spot-project-request-to-notion/)): an existing contact is only ever *filled in* (message → `Note` when empty, else a page comment; `Mailing List` only ever ticks), a new one is created **with the Contacts default template** and its address indexed into the Table immediately so a repeat submission can't mint a duplicate. Emails lowercased + validated before lookup. Verified live with a scratch address across both paths (cleaned up after). **Cutover pending and urgent: the classic Zap subscribes to the same form, so every submission double-processes — and the classic creates a duplicate contact each time — until it is turned off.** |
| [`esignatures-status-to-notion`](esignatures-status-to-notion/) | Durable ×2 | ✅ Enabled | eSignatures fires → look the contract id up in the mapping Table → move the mapped SOW or Project Addendum's status on. One shared `shared.ts` deployed twice — `esignatures-contract-sent-to-notion` (`contract_sent_to_signer`) and `esignatures-contract-signed-to-notion` (`contract_signed`) — replacing the classic "Update SOW / Project Addendum Status When Sent for Signature" and "Signed SOWs / Project Addenda". **Now actually files the executed PDF**, which the classic Zap only appeared to do: it passed `properties|||Signed PDF|||files: []`, an empty array and a silent no-op, so no executed PDF was ever filed on any record. Getting it filed *permanently* ruled out both obvious routes — handing the URL to the files property stores a `{type:"external"}` link that Notion never re-hosts and which dies with the **72-hour** presigned URL, and Notion's `external_url` upload mode probes with `HEAD`, which an S3 URL signed for `GET` answers with 403 — so the bytes are downloaded and pushed through a `single_part` Notion upload, landing as a permanent Notion-hosted file. Three gotchas are documented in the directory README: the multipart part must restate the upload's content type, a files-entry `name` is capped at 100 characters, and **the durable sandbox has no DNS for arbitrary hosts** (a bare `fetch` dies with `getaddrinfo ENOTFOUND`, so egress goes through `sdk.fetch`). Also fixes the classic Zap's lookup failing on a miss, which turned every contract created outside these flows into an error alert — now a clean skip. The two triggers disagree about where the contract id lives (`.contract.id` vs `.id`) and both paths are read. Filing never throws and surfaces `pdfError` in the run **output**, because a durable run's console logs are not retrievable from `get-durable-run`. Verified against the live NUS (LKYSPP) addendum, where it **fixed real missing data** — already `Executed` from the classic Zap but its PDF had never been filed. **Already live in parallel** with the classic Zaps (both write the same status, so the duplicate write is idempotent); the one open item is tightening the PDF-URL field name after the first real `contract_signed`, since no CLI command exposes a trigger's output shape — the deep-scan fallback is tested and covers it meanwhile. |
| [`notion-blog-post-to-hero-image`](notion-blog-post-to-hero-image/) | Durable | ✅ Enabled | Notion Blog **Generate Image** button → generate a brand-aligned hero image and attach it twice: as the page **cover** and in the **Cover Image File** files property. Migration of the classic "Generate Blog Post Image". A filled `Prompt (Optional)` is passed to Gemini (Nano Banana Pro, `v1beta`) verbatim; otherwise the post body is read as markdown and an **AI by Zapier `standard/auto`** step writes the brief — the classic Zap's raw-Anthropic step (Claude Opus on Dennis's own billing) with a brand-guidelines PDF knowledge source is replaced by the guidelines embedded in [`notion-blog-post-to-hero-image-prompt.md`](notion-blog-post-to-hero-image/notion-blog-post-to-hero-image-prompt.md), verified on-brand at Standard on a real post. **The cover is now a Notion-hosted file** (bytes downloaded via `sdk.fetch` no-connection, `single_part`-uploaded, attached as `file_upload`) — the classic Zap's custom action stored an external Gemini URL that rots when the link expires. The Gemini step returns **only the image URL**, since the raw result can carry binary that a step checkpoint must never hold. Verified end-to-end on a scratch Blog page across both prompt paths, plus the empty-ping skip; scratch page trashed after. **Cutover done 2026-08-14** — the Blog DB's Generate Image button posts to this durable's catch URL and the classic Zap is off (confirmed by Dennis, not machine-verifiable). |
| [`blog-rss-to-slack`](blog-rss-to-slack/) | Durable | ✅ Enabled | Blog RSS feed (`blog/index.xml`, smart polling) → announce the new post in Slack **#marketing** as **BlogBot**, filtering out the feed's `/blog/tags` and `/blog/authors` index entries exactly as the classic "Share new Blog Posts in Slack" did. Message presentation is identical (`New <link\|blog post> published`, `:blogger:` icon, unfurl on). A payload with content but no link throws loudly. Verified via `run-durable`: tag-page payload skips, real-post payload builds the exact classic message (`previewOnly`, nothing posted). Published disabled to avoid double-posting alongside the classic Zap; **cutover done 2026-08-14** — the classic Zap is off (confirmed by Dennis, not machine-verifiable) and this durable is enabled. Polling has no backfill, so anything published inside the brief flip gap goes unannounced. |
| [`drive-files-to-zapier-table`](drive-files-to-zapier-table/) | Durable | ✅ Enabled | Google Drive polling trigger (new file/folder anywhere on the **Work.Flowers HQ** shared drive, 4 subfolder levels deep) → upsert the object into `[Table] Google Drive Files and Folders`. Migration of the classic "Log New Google Drive Files and Folders in Zapier Table", which called `create_record` unconditionally and so **duplicated a row on every re-trigger**; the durable upserts on the Drive id, leaves a known id alone, repairs one whose name or parent drifted, and converges racing creates on the earliest ULID. Zero tasks per run — every Table call is free. **Cutover done 2026-08-22** — the classic Zap is off (confirmed by Dennis, not machine-verifiable) and this durable is enabled. |
| [`drive-file-renamed-to-zapier-table`](drive-file-renamed-to-zapier-table/) | Durable | ✅ Enabled | Google Drive polling trigger (file/folder updated) → keep that object's `Name` and `Parent ID` current in `[Table] Google Drive Files and Folders`. Migration of the classic "Update Google Drive File Names in Zapier Table". The classic Zap folded "name differs" into its Table search filter with success-on-miss **off**, so every update that changed something *other* than the name — and every update to a file the inventory had never logged — failed the step and raised a Zap error; the durable looks the row up by id alone, compares in code, and skips quietly. Also mirrors moves (`Parent ID`), and never creates rows — that stays [`drive-files-to-zapier-table`](drive-files-to-zapier-table/)'s job. Zero tasks per run. **Cutover done 2026-08-22.** |
| [`company-ids-table-to-fivetran-sheet`](company-ids-table-to-fivetran-sheet/) | Durable | ✅ Enabled | Schedule trigger (Sundays 00:00 SGT) → clear and rewrite the **"Company IDs"** tab of the **Fivetran Sync Jobs** spreadsheet from every row of `[Table] Company IDs`. Migration of the classic "Refresh Company IDs Table in Fivetran Sheet", whose delete range was computed from the count of the *new* data (`2-(N+1)`) — so a shrinking Table left orphan rows below the fresh data and a growing one deleted rows it was about to rewrite. The durable clears `A2:L` outright and appends the full set through the Sheets API (`GoogleSheetsV2CLIAPI` `_zap_raw_request`, so the calls keep the connection's auth), which also **grows the grid** when the Table has outgrown the sheet. An empty Table read now **aborts rather than blanking Fivetran's source**, and a short write throws instead of leaving a truncated tab. 2 tasks a week instead of 4 steps, since the Table read is free. The tab *title* and its A–L column order are contracts — `COLUMNS` in `workflow.ts` must track the header row. **Cutover done 2026-08-22**; the first scheduled run is the coming Sunday at 00:00 SGT. |
| [`notion-company-renamed-to-xero-harvest-drive`](notion-company-renamed-to-xero-harvest-drive/) | Durable | ✅ Enabled | Notion Companies polling trigger (**Company Name** changed) → rename the company in **Xero**, **Harvest**, **Google Drive** and `[Table] Company IDs`, each only where that system's id sits on the page. Migration of the classic "Sync Company Name Updates to Xero, Harvest, and Zapier Table". Reads the four ids off the Notion page instead of `[Table] Company IDs` — fresher, one fewer lookup, and a missing Table row no longer halts the whole run (same precedent as [`xero-contact-from-notion-deal`](xero-contact-from-notion-deal/)). The classic Zap's four independent paths are preserved with `Promise.allSettled` over four retrying steps, so a Xero failure no longer blocks the Harvest, Drive and Table renames — and the run still fails loudly, naming which targets succeeded. A blank Company Name now skips rather than renaming a Xero contact or Drive folder to `""`. Xero/Harvest renames use the same account-defined custom actions the classic Zap called (`ae:515080`, `ae:541562`). **Cutover done 2026-08-22.** |
| [`linear-customer-to-notion-company`](linear-customer-to-notion-company/) | Durable | ✅ Enabled | Linear instant trigger (**new customer**) → find or create the matching Notion **Companies** record by domain and write the Linear customer id onto that page; [`notion-companies-to-zapier-table`](notion-companies-to-zapier-table/) mirrors it into `[Table] Company IDs`. Migration of the classic "Update New Linear Customer in Zapier Table". **Fixes the bug that put domains in the id field:** the classic Zap's fallback mapped `{{domains}}` into `f7 Linear Customer ID`, which is why existing rows carry hostnames like `tanninroad.com.au` — the same defect already recorded against [`notion-company-to-linear-customer`](notion-company-to-linear-customer/). Writes to the Notion page rather than the Table, so the mirror keeps a single owner; matches the hostname **exactly** (the classic `icontains` on `f9 Domain` could match `acme.com` to `notacme.com`); creates companies with the default template (repo rule 5); and guards on the id already being set, which is what makes the loop with `notion-company-to-linear-customer` terminate. **Cutover done 2026-08-22** — the classic Zap was disabled first (the two resolve companies differently, so an overlap could have created duplicate Notion Companies), then this durable enabled. |
| [`agent-chat-session-to-notion`](agent-chat-session-to-notion/) | Durable | 🆕 Pending create | `session.ended` webhook from the [embeddable-agent-chat](https://github.com/work-flowers/embeddable-agent-chat) Lovable app (widget chat ended: idle rollover or message cap) → fetch the full transcript via the **Notion Agents Unofficial** custom integration (`App245513CLIAPI`) → AI by Zapier summary (`standard/auto`, verified on 3 real sessions) → record in the **Agent Chat Sessions** Notion database (data source `64e3a5e5`, under Marketing → Databases) with the transcript as the page body. Empty pings of the catch URL skip; a non-empty payload with no extractable session id throws (repo default). Accepts rare duplicate pages per the repo's idempotency posture — the sender's `event_id` is stored in `Event ID` so duplicates are findable. **Cutover after first publish:** paste the catch `webhook_url` into the widget's "Webhook URL" field in the Lovable app's UI. |

### Classic Zaps deliberately not migrated

| Classic Zap | Why |
| --- | --- |
| **"Update Zapier Table When Customer Added to Project"** | Retired 2026-08-22 instead of migrated. Its only output was `[Table] Project IDs` (`01JM22XAKXJH4QM9925KSKQEHV`), which **no longer exists** in the account, and nothing in this repo reads a Linear project → customer mapping. (`[Table] Linear Projects` and `[Table] Deal <-> Project Mapping` both exist but carry no customer column.) It was a catch-hook Zap fed by a Linear webhook — so if the mapping is ever wanted again, build it deliberately against a live table, ideally off Linear's own `newCustomerNeedInstant` trigger rather than a hand-wired webhook. Confirmed with Dennis 2026-08-22. |

## Working with these Zaps

Managed via the Zapier SDK CLI (preferred — faster and more cost-effective) or the Zapier MCP connector as a fallback:

- **List deployed workflows** — CLI workflows list command (MCP: `list_workflows`)
- **Inspect a version** — CLI workflows version command (MCP: `get_workflow_version`, returns `source_files`)
- **Publish changes** — edit `workflow.ts` on a branch and merge a PR to `main`; the [`Publish Zaps`](.github/workflows/publish-zaps.yml) GitHub Action republishes every affected deployment automatically (merging the PR is the approval — the former `production`-environment gate was removed 2026-08-18) and commits the new `current_version_id` back into `zap.json`. Don't run `publish-workflow-version` or hand-edit `current_version_id` on this path — a direct CLI publish bypasses PR review and the auto sync-back, and is reserved for stated out-of-band hotfixes. See "Publishing to Zapier" in [CLAUDE.md](CLAUDE.md), or the **[visual pipeline explainer](https://work-flowers.github.io/zapier-sdk/zap-publishing-pipeline.html)** (merge → detect → publish → sync-back, with the detection rules and the publisher's refusal conditions).
- **Trigger changes ship the same way too** — `zap.json` is the source of truth for a Zap's trigger, and the trigger is the one piece of metadata *not* carried forward from the live version. Every republish compares the declared trigger against the deployed one on the four fields that are Zapier's (`selected_api`, `action`, `authentication_id`, `params`); identical, the deployed object goes back verbatim, different, the declared one replaces it. So a PR touching only `zap.json` is a trigger *candidate*: the publish job runs, compares, and republishes only the deployments whose trigger actually differs (a note-only tweak is a no-op). This also means a trigger edited in the Zapier editor gets healed back to what the repo says — run `node scripts/publish-changed-zaps.mjs --audit` to see every such mismatch without publishing anything. A changed catch-hook trigger can be issued a new `webhook_url`; the pipeline syncs it back, but repointing whatever POSTs to the old URL is a manual step.
- **Ship a brand-new Zap the same way** — a directory whose `zap.json` has no `workflow_id` and a `deploy` block with `"state": "pending-create"` is *created* on merge: the pipeline runs `create-workflow` (honouring `is_private`, the only chance to set visibility — repo rule 7), publishes v1 from the metadata declared in that block, disables the workflow when `deploy.enable_on_publish` is `false`, verifies the trigger claim survived, and syncs `workflow_id` / `current_version_id` / `trigger_url` / `enabled` back plus flipping `deploy.state` to `published`. Because there is no live version to read metadata off, every field is required to be explicit — the publisher refuses rather than defaulting visibility, start mode or enabled state, and rejects a `trigger.selected_api` that is not version-pinned (an unversioned one fails the claim silently). So you never run `create-workflow` or `publish-workflow-version` by hand for a new Zap either.
- **Webhook-trigger Zaps have two URLs** — the top-level `trigger_url` (`code-substrate-workflows.zapier.com/…`) is Zapier-internal (manual `trigger-workflow` runs, Zap-to-Zap calls). External services must POST to the **catch URL** (`hooks.zapier.com/hooks/catch/…`), retrieved via `get-workflow <id>` at `.data.triggers[0].details.webhook_url` and recorded in `zap.json` as `trigger.webhook_url`.

Convention: the repo must always match what is deployed. On the merge path the pipeline keeps `current_version_id` in sync for you; for a direct out-of-band publish, or any metadata beyond `current_version_id` (enabled state, dependency versions, `version_note`), sync `zap.json` back by hand so it stays the source of truth.

### Setting up the Zapier CLI

Requires Node.js 20+. The CLI runs via `npx` — no global install needed:

```bash
npx zapier-sdk login
```

This opens a browser to authenticate against your Zapier account (credentials are stored at `~/.config/zapier-sdk-cli-nodejs/config.json`). On a machine without a browser, use `npx zapier-sdk login --headless`. For project-local installs: `npm install -D @zapier/zapier-sdk-cli` (and `@zapier/zapier-sdk` as a runtime dependency if writing code against the SDK).

**Durables note:** the Code Workflows commands used for these Zaps are still experimental — use the `zapier-sdk-experimental` bin, or pass `--experimental` / set `ZAPIER_EXPERIMENTAL=true` with the plain `zapier-sdk` CLI.

Reference docs:

- [Zapier SDK API reference](https://docs.zapier.com/sdk/reference)
- [SDK changelog](https://docs.zapier.com/sdk/changelog) (Zapier ships near-daily — check here when a command's behaviour seems off)

### Agent skills

Zapier's own agent skills are vendored here so a session doesn't have to work from memory — `zapier-sdk` (from `zapier/sdk`) plus `workflows-create` / `-modify` / `-doctor` / `-list` / `-history` / `-install` (from `zapier/agent-skills`). Versions are pinned by content hash in [`skills-lock.json`](skills-lock.json).

```bash
npx skills add zapier/sdk --skill zapier-sdk       # install or reinstall one
npx skills update --project                        # refresh all of them
```

They install to **`.agents/skills/`** — the cross-agent location, readable by Cursor and Codex as well. Claude Code only discovers project skills under **`.claude/skills/`**, so each is committed there as a relative symlink:

```
.claude/skills/zapier-sdk -> ../../.agents/skills/zapier-sdk
```

That is what makes them invocable as `/zapier-sdk`, `/workflows-modify` and so on; without it they sit on disk unused and `Skill(zapier-sdk)` fails with `Unknown skill`. The [`link-agent-skills.sh`](.claude/hooks/link-agent-skills.sh) SessionStart hook keeps the two in step — it creates links for newly installed skills, repairs broken ones (including a clone that materialised the symlink as a plain text file), prunes links to skills that are gone, and still warns when `zapier-sdk` isn't installed at all. A skill linked mid-session may only appear in the skill list after a restart.

> **`.agents/skills/` is the source of truth — don't edit a skill through the link.** `npx skills add`/`update` rewrites that directory and `skills-lock.json` wholesale, and would discard the change.
