{
  "name": "xero-bank-transactions-to-zapier-table",
  "workflow_id": "019fa885-7d5e-73a8-b601-4ec31290bf4a",
  "current_version_id": "019fdb43-253f-7e97-890d-c5dc01f3abf2",
  "trigger_url": "https://code-substrate-workflows.zapier.com/api/v0/workflows/trigger/AaVB57afDcrtlFL0dbihDA",
  "enabled": true,
  "is_private": false,
  "trigger": {
    "selected_api": "ScheduleCLIAPI@1.7.0",
    "action": "everyHour",
    "authentication_id": null,
    "authentication_note": "NONE \u2014 `get-app ScheduleCLIAPI` reports `auth_type: \"\"`, so Schedule by Zapier needs no connection. The Xero credential moved OFF the trigger and into the workflow body as the `xero_wf` connection alias, because the body now reads Xero itself.",
    "params": {
      "moh": "00",
      "weekends": "yes"
    },
    "params_note": "`moh` (Time Offset) MUST be the STRING \"00\", not the integer 0, even though `list-trigger-input-fields` declares it `value_type: INTEGER`. Its real choices are the zero-padded strings \"00\"/\"15\"/\"30\"/\"45\" (`list-trigger-input-field-choices ScheduleCLIAPI everyHour moh`). Publishing with the integer 0 was REJECTED \u2014 `ZAPIER_VALIDATION_ERROR: '0' is not an allowed value for 'moh'` \u2014 which is worth knowing: a bad trigger param is documented elsewhere in this repo as failing SILENTLY at publish, but a value outside a SELECT's choice set is caught loudly. Do not rely on either behaviour; verify `triggers[0].status == \"active\"` after every publish.",
    "status": "active",
    "trigger_note": "SCHEDULE trigger \u2014 fires hourly at the top of the hour, weekends included. It replaced the Xero `bank_transaction` POLLING trigger on 2026-08-06 to stop this Zap consuming Xero API quota; see xero_api_usage. The Zapier-internal subscription URL is https://hooks.zapier.com/hooks/standard/20495893/ff5fac39f8f34e13b5aa34e69a4866ad/ \u2014 that is Zapier's own plumbing for the schedule subscription, NOT a catch URL for any external service to call.",
    "why_not_polling": "A durable trigger has no polling-interval field: the `--trigger` payload accepts only selected_api / action / authentication_id / params, verified across all 43 live triggers in the account. So the classic Zap's `polling_interval_override: 5` had nowhere to go at migration and this Zap silently began polling at the account default (~1 min). A schedule trigger costs Xero nothing and puts the interval back under our control \u2014 the Xero read moved into the body where WE decide the cadence.",
    "manual_run_note": "`trigger-workflow 019fa885-7d5e-73a8-b601-4ec31290bf4a --input '{\"dryRun\":true}'` computes the whole pass and reports what it WOULD write, writing nothing. Added in 019fdb43 and NOT optional discipline: the version that wiped the date column was 'validated' by a run-durable that died at the Xero fetch on a rate limit, so the extraction and upsert code was never executed once before reaching production. Use the dry run to exercise the main path against real Xero data before any publish. An empty input `{}` is a real re-sync."
  },
  "connections": {
    "xero_wf": "02336808-1736-878b-a0a8-87e02bb0aec3"
  },
  "connections_note": "NEW as of 019fd4b2. The body now calls Xero (`_zap_raw_request` GET /BankTransactions), so the credential is bound as an alias rather than living on the trigger. Note the shape mismatch: reads return {\"xero_wf\":{\"connection_id\":\u2026}} (snake_case) while the publish/update flags take {\"xero_wf\":{\"connectionId\":\u2026}} (camelCase). `app_versions` is deliberately omitted \u2014 drive-invoice-to-xero also calls XeroCLIAPI via runAction with `app_versions: null`.",
  "connectionless_apps": {
    "ZapierTables": "read/write via sdk.listTableRecords / createTableRecords / updateTableRecords / deleteTableRecords \u2014 no connection, no task cost",
    "ScheduleCLIAPI": "the trigger \u2014 auth_type \"\", no connection, and it never touches Xero"
  },
  "xero_api_usage": {
    "before": "~1,440 calls/day. The `bank_transaction` polling trigger polled Xero at the account default interval, every day, whether or not anything had changed.",
    "after": "~24 calls/day \u2014 CONFIRMED 2026-08-07 on the converged run: xeroPagesRead 1, transactionsFetched 22 over a 7-day window, updated 0. One hourly fire = 1 Xero call in steady state (2-3 only if a window ever spans more than 100 transactions).",
    "why_it_mattered": "Xero rate-limits per TENANT: 60 calls/min and 5,000 calls/day. Five Xero polling triggers on tenant 62699a8c exhausted the daily limit on 2026-08-06 \u2014 confirmed from Xero's own response headers, `x-rate-limit-problem: day` with `x-daylimit-remaining: 0` and `x-minlimit-remaining: 60`, i.e. the per-minute budget was completely untouched, so it was NOT a burst, a concurrency problem or a retry storm. Every Xero Zap in the workspace was down for ~10 hours (retry-after 33519s) and drive-invoice-to-xero dropped two invoices.",
    "pattern": "A schedule trigger is the interval knob durable pollers do not have. Cost moves from 'polls per day' to 'fires per day', and the fire rate is ours to set."
  },
  "zapier_durable_version": "0.10.1",
  "dependencies": {
    "zod": "4.4.3",
    "@zapier/zapier-sdk": "0.91.0"
  },
  "version_pinning_note": "@zapier/zapier-durable 0.11.0 was npm-latest at build time but only ~20h old; the runtime rejects dependencies younger than 24h at RUN time, not publish. Pinned to 0.10.1.",
  "tables": {
    "bank_transactions": {
      "id": "01KCDV6Y17F31J2Q6S1EMYZC8K",
      "name": "Xero Bank Transactions",
      "role": "Owned and written by this workflow.",
      "key_field": "bank_transaction_id",
      "fields": {
        "bank_transaction_id": "id (== Xero BankTransactionID)",
        "date": "Date, pinned to YYYY-MM-DDT00:00:00Z",
        "bank_account_id": "BankAccount.AccountID",
        "reference": "Reference",
        "contact_id": "Contact.ContactID (empty on transfers)",
        "contact_name": "Contact.Name (empty on transfers)",
        "type": "Type (labeled_string \u2014 a plain string is normalised to {value,label})",
        "currency_code": "CurrencyCode (labeled_string)",
        "currency_rate": "CurrencyRate \u2014 NEW: the classic Zap never mapped f9, so this column was null on all 651 rows despite being populated on ~36% of transactions",
        "total": "Total",
        "has_attachments": "HasAttachments"
      },
      "downstream": "drive-invoice-to-xero reads this Table to decide whether an invoice has already been paid. A pause here is a silent production fault there \u2014 it produces duplicate draft bills with no error. Treat this workflow as load-bearing."
    }
  },
  "date_coercion_note": "The `date` column is written as YYYY-MM-DDT00:00:00Z. A bare YYYY-MM-DD is interpreted in the account's local timezone (Asia/Singapore) and lands 8 hours off, which shifts every date-window query run against this Table by a day at the boundaries.",
  "description": "Hourly schedule -> read Xero bank transactions dated in the [newest mirrored row - 7d, today] window -> find-or-create/refresh each row in the 'Xero Bank Transactions' Zapier Table, keyed on bank_transaction_id. Migration of the classic Zap 'Process New Reconciled Transactions', which created unconditionally. Adds: find-or-create, race-safe dedupe of concurrent creates (converge on the earliest ULID), refresh of an existing row when a mirrored field has actually changed, and the previously-unmapped currency_rate column. Since 019fd4b2 it is schedule-driven rather than Xero-poll-driven, which cut its Xero API cost ~97% and made it self-healing.",
  "dedupe_note": "NO separate state table is needed, and none was created. The mirror Table IS the dedupe state: each transaction is looked up by its own key (`bank_transaction_id`) to decide create-vs-update, and stray rows are deleted so the key stays unique. This is why bank_transaction was the cheapest poller to convert \u2014 an ALERTING Zap (xero-draft-bill-to-slack-alert, xero-draft-sales-invoice-to-slack-alert) writes nothing, so it depends entirely on Zapier's built-in polling dedupe and needs new state of its own before it can move to a schedule.",
  "version_history": [
    {
      "id": "019fa885-b4c5-764e-9cc8-7cb86ecb284e",
      "created_at": "2026-07-28T11:36:00.000Z",
      "note": "Initial migration."
    },
    {
      "id": "019fd4b2-925a-75a7-9461-484908b10f5a",
      "created_at": "2026-08-06T01:31:40.732Z",
      "note": "Xero POLLING trigger -> SCHEDULE trigger. Replaces XeroCLIAPI@2.20.5/bank_transaction with ScheduleCLIAPI@1.7.0/everyHour and moves the Xero read into the body: a bounded GET /BankTransactions over [newest mirrored row - OVERLAP_DAYS(7), today], hard-capped at MAX_PAGES(3) and MAX_TRANSACTIONS(200), warning loudly rather than truncating silently. Cuts Xero API cost from ~1,440 calls/day to ~24-48 \u2014 the reason for the change; see xero_api_usage. Binds the `xero_wf` connection for the first time (the body never called Xero before). The upsert, dedupe, snapshot and change-detection logic is unchanged. ALSO FIXES THREE KNOWN GAPS, all as a side effect of re-reading a window instead of consuming a dedupe store: (1) no_backfill_by_design \u2014 a failed run, or an hour the Zap spent disabled, is simply re-read next fire; (2) stale_columns \u2014 recent rows are re-read hourly, so the update path finally corrects the `reference` / `currency_rate` / `has_attachments` values Xero fills in AFTER creation; (3) it removes the staleness that made drive-invoice-to-xero raise duplicate draft bills. Validated before publish: tsc clean; determinism grep clean (the only Date.now() is inside ctx.step(\"today\")); every sdk.* call reachable only from inside a ctx.step; run-durable in the real 0.10.1 runtime got through `today` (2026-08-06), `latest-mirrored-row` (newest row 2026-08-04) and built the correct window `Date>=DateTime(2026,7,28)` with NO DeterminismViolation, then failed at `fetch-bank-transactions` on the Xero daily-quota 429 that prompted this whole change. Verified after publish: triggers[0].status active, enabled true, 0 open drafts, deployed source byte-identical to the repo. NOT YET EXERCISED BY A SUCCESSFUL XERO READ \u2014 the daily quota does not reset until ~10:11 UTC 2026-08-06, so every fire before then fails harmlessly at the fetch step and self-heals on the next one."
    },
    {
      "id": "019fdb43-253f-7e97-890d-c5dc01f3abf2",
      "created_at": "2026-08-07T08:07:18.784Z",
      "note": "INCIDENT FIX \u2014 019fd4b2 was silently WIPING the `date` column. Root cause: `date: toTableDate(p.Date ?? p.DateString)` combined with a `toIsoDate` that accepted only `YYYY-MM-DD`. Xero's raw JSON API returns `Date` as .NET epoch form (\"/Date(1784937600000+0000)/\") and only `DateString` as ISO \u2014 and because `p.Date` is always present, `??` never fell through to `DateString`. So every snapshot got `date: null`, `changedFields` reported a difference against the good stored date, and the update path wrote the null over it. 214 of 685 rows lost their date. The corruption was self-amplifying: the workflow derives its own read window from the newest mirrored row, so as dates were wiped the apparent newest row receded (2026-02-24 -> 02-17 -> 02-05), the window ballooned to ~6 months, and it burned 3 Xero pages an hour instead of 1 while churning 13-17 rows. This ALSO silently broke coverage: with MAX_TRANSACTIONS 200 against 3 pages it fetched 215 and processed only the first 200 sorted by transaction id, an arbitrary subset. Downstream, drive-invoice-to-xero matches payments on a date window, so a null-date row can never match and would have produced duplicate draft bills \u2014 the very symptom this whole migration was meant to remove. FOUR CHANGES: (1) toIsoDate now parses the .NET epoch form, matching xero-invoice-alerts, which had a correct parser all along; (2) date extraction is `toTableDate(p.DateString) ?? toTableDate(p.Date) ?? toTableDate(p.date)` \u2014 each candidate tried independently, so it is no longer order-dependent on which key happens to be non-null; (3) NEW GUARD `wouldBlankAPopulatedField` \u2014 a blank extracted value never overwrites a populated stored one, in BOTH changedFields and the update payload (excluding it from the diff alone is not enough, because the write sent the whole snapshot). This guard would have prevented the incident on its own, independently of the parser bug: a mirror should only ever add or correct, never delete. It logs a WARNING naming the preserved fields. `has_attachments` is exempt because `false` is a real value there, not an absence; (4) MAX_TRANSACTIONS is now MAX_PAGES * 100 so a run can never cap below what Xero returned. Also adds a `dryRun` input. VERIFIED: dry run reached the main path and reported updated 214 / unchanged 1 / created 0 over 215 transactions, writing nothing; the real repair run completed 432 operations with 214 update-row steps and took rows-without-a-date from 214 to 0, restoring the range to 2020-02-26..2026-08-06 with the row count unchanged at 685; the next run then CONVERGED \u2014 windowFrom 2026-07-30 (exactly OVERLAP_DAYS behind the true newest row), xeroPagesRead 1, transactionsFetched 22, created 0, updated 0, unchanged 22, xeroCoverageIncomplete false."
    }
  ],
  "replaces_classic_zap": {
    "title": "Process New Reconciled Transactions",
    "old_shape": "XeroCLIAPI@2.20.5 bank_transaction trigger -> TableCLIAPI@1.25.0 create_record (unconditional), mapping f1-f8, f10, f11",
    "known_issues_fixed": [
      "Created unconditionally, so a re-delivery duplicated the row \u2014 the Table holds 4 duplicate pairs across 651 distinct transactions.",
      "f9 (currency_rate) was never mapped.",
      "No way to correct a row after Xero restated the transaction or an invoice was attached to it."
    ],
    "status": "Its create step was PAUSED, which is why the Table fell ~5 days behind and drive-invoice-to-xero began raising duplicate draft bills. Turn the classic Zap off entirely \u2014 do not simply unpause it, or both it and this workflow will write the same rows."
  },
  "known_gaps": {
    "no_backfill_by_design": "RESOLVED in 019fd4b2 for anything going forward. This was true of the POLLING trigger: it primed its dedupe on first poll, so a gap stayed a gap and re-enabling never recovered it. The schedule trigger has no dedupe store \u2014 it re-reads [newest row - 7d, today] every fire \u2014 so a failed run or an hour spent disabled self-heals on the next fire, up to OVERLAP_DAYS(7) deep. HISTORICAL GAP STILL UNFILLED: the ~7 SPEND transactions missing from 2026-07-21..2026-07-26 (including Anthropic 2026-07-25 SGD 104.58) predate this change and sit outside any current window. Still accepted \u2014 they were reconciled against their receipts in Xero by hand and their invoices have already passed through the Drive Invoices folder. To recover them deliberately, temporarily raise OVERLAP_DAYS or run a one-off with a wider window.",
    "stale_columns": "LARGELY RESOLVED in 019fd4b2 for recent rows. Xero fills some values in AFTER a transaction is created \u2014 sampled over 93 transactions, 32 had a stale currency_rate, 26 a stale reference, 14 a stale has_attachments, 1 a renamed contact \u2014 and the update path only fires when a transaction is re-read. The polling trigger almost never re-delivered, so these persisted; the schedule window now re-reads everything within OVERLAP_DAYS(7) every hour, so a value Xero backfills within a week of creation is now picked up. Rows OLDER than the current window keep their stale values, which remains accepted: no consumer reads those columns (drive-invoice-to-xero matches on contact_name, type, currency_code, total and date, none of which drift).",
    "restatement_of_old_transactions": "NEW, and the one thing the schedule window is worse at in principle: a transaction restated MORE than OVERLAP_DAYS after its date falls outside every window and is never re-read. The polling trigger was no better \u2014 'New Bank Transaction' fires on creation, not restatement \u2014 so this is not a regression, just a bound worth knowing. If it ever matters, switch the fetch to Xero's `If-Modified-Since` header, which selects on modification time rather than transaction date.",
    "next_incident": "The mechanic worth remembering has changed. It is no longer 'a pause is silent and the gap is permanent' \u2014 the window self-heals. It is now: this Zap makes ~24-48 Xero calls/day, and the tenant shares a 5,000/day ceiling with every other Xero Zap. If Xero throttling returns, check the whole tenant's poller inventory before blaming this Zap, and read Xero's `x-rate-limit-problem` / `x-daylimit-remaining` headers rather than guessing from the error text."
  },
  "created_at": "2026-07-28T11:35:00.000Z",
  "version_created_at": "2026-08-07T08:07:18.784Z"
}
